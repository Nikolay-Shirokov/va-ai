# Руководство по семантической валидации для AI-ассистента

> **Версия:** 1.0  
> **Для:** AI-ассистенты работающие с Vanessa Automation  
> **Цель:** Предотвращение зацикливания и неправильного применения рекомендаций валидатора

---

## 🎯 Главное правило

**НИКОГДА не применяйте рекомендацию валидатора без семантической проверки!**

Высокая алгоритмическая схожесть (85%+) НЕ означает семантическую релевантность.

---

## 🔍 Обязательный чек-лист перед применением рекомендации

Для **каждой** рекомендации проверьте ВСЕ пункты:

```
☐ 1. Действие совпадает? (нажимаю vs ввожу vs выбираю)
☐ 2. Тип UI элемента совпадает? (кнопка vs гиперссылка vs поле)
☐ 3. Контекст совпадает? (в таблице vs в форме vs без контекста)
☐ 4. Параметры совместимы? (заголовок vs имя элемента)

Если ВСЕ ✅ → можно применять
Если хотя бы один ❌ → НЕ применять, спросить пользователя
```

---

## 📋 Детальные правила проверки

### 1. Проверка ДЕЙСТВИЯ

#### Категории действий

```yaml
Группа "Клик/Нажатие":
  - нажимаю
  - кликаю  
  - выбираю (на элемент)
  - щелкаю
  
  ✅ Взаимозаменяемы внутри группы
  ❌ НЕ заменяются на другие группы

Группа "Ввод данных":
  - ввожу
  - устанавливаю
  - задаю
  - заполняю
  
  ✅ Взаимозаменяемы внутри группы
  ❌ НЕ заменяются на другие группы

Группа "Выбор из списка":
  - выбираю из списка
  - выбираю из выпадающего списка
  - выбираю по строке
  - выбираю точное значение
  
  ✅ Взаимозаменяемы с учетом параметров
  ❌ НЕ заменяются на "Клик" или "Ввод"

Группа "Проверки":
  - проверяю
  - сравниваю
  - убеждаюсь
  - жду
  
  ✅ Взаимозаменяемы внутри группы

Группа "Навигация":
  - открываю
  - перехожу
  - закрываю
  - активизирую
  
  ⚠️ Каждое действие специфично, осторожно
```

#### Примеры проверки действия

```gherkin
# ✅ ПРАВИЛЬНО - действие совпадает
Оригинал:    "я нажимаю кнопку"
Рекомендация: "я нажимаю на кнопку"
Действие: нажимаю → нажимаю ✅
```

```gherkin
# ❌ НЕПРАВИЛЬНО - разные действия!
Оригинал:    "я ввожу в поле"
Рекомендация: "из выпадающего списка я выбираю"
Действие: ввожу → выбираю ❌

⚠️ Это РАЗНЫЕ действия! Тест будет работать некорректно!
→ НЕ применять, обратиться к пользователю
```

```gherkin
# ❌ НЕПРАВИЛЬНО - схожие слова, но разная семантика
Оригинал:    "я выбираю кнопку"  (клик)
Рекомендация: "я выбираю из списка"  (выбор значения)
Действие: выбираю (клик) → выбираю (из списка) ❌

⚠️ Слово одинаковое, но КОНТЕКСТ разный!
→ НЕ применять
```

---

### 2. Проверка ТИПА UI ЭЛЕМЕНТА

#### Иерархия элементов

```
Кнопочные элементы (взаимозаменяемы):
  ├─ кнопка
  ├─ кнопка с именем
  ├─ кнопка командного интерфейса
  └─ кнопка выбора

Поля ввода (взаимозаменяемы):
  ├─ поле
  ├─ поле с именем
  └─ поле ввода

Элементы выбора (НЕ взаимозаменяемы):
  ├─ выпадающий список
  ├─ список
  ├─ дерево
  └─ переключатель

Ссылочные элементы (НЕ взаимозаменяемы с кнопками!):
  ├─ гиперссылка
  └─ навигационная ссылка

Табличные элементы:
  ├─ таблица
  └─ табличная часть

Элементы форм:
  ├─ форма
  ├─ панель
  └─ группа
```

#### Примеры проверки элемента

```gherkin
# ✅ ПРАВИЛЬНО - элементы одной категории
Оригинал:    "нажимаю кнопку"
Рекомендация: "нажимаю на кнопку с именем"
Элемент: кнопка → кнопка с именем ✅
(разные селекторы, но тот же тип элемента)
```

```gherkin
# ❌ КРИТИЧЕСКАЯ ОШИБКА - разные типы элементов!
Оригинал:    "нажимаю на кнопку \"Создать\""
Рекомендация: "нажимаю на гиперссылку \"Создать\""
Элемент: кнопка → гиперссылка ❌

⚠️ Это РАЗНЫЕ элементы интерфейса!
Кнопка ≠ Гиперссылка
Схожесть 87% - ОБМАНЧИВА!

→ НЕ применять, обратиться к пользователю:
   "Валидатор предлагает заменить кнопку на гиперссылку.
    Это разные UI элементы. Что в вашем интерфейсе?
    1. Кнопка (обычная кнопка)
    2. Гиперссылка (текстовая ссылка)"
```

```gherkin
# ❌ НЕПРАВИЛЬНО - разные элементы ввода
Оригинал:    "в поле \"Наименование\" я ввожу"
Рекомендация: "из выпадающего списка \"Наименование\" я выбираю"
Элемент: поле → выпадающий список ❌

⚠️ Поле ввода и выпадающий список - РАЗНЫЕ элементы!
→ НЕ применять
```

```gherkin
# ⚠️ ОСТОРОЖНО - может быть ок, нужна проверка
Оригинал:    "в поле \"Покупатель\" я ввожу"
Рекомендация: "из выпадающего списка \"Покупатель\" я выбираю"

Анализ:
- В 1С поля с выбором могут работать и как поле ввода, и как список
- Но это РАЗНЫЙ способ заполнения

→ Обратиться к пользователю:
   "Поле 'Покупатель' в вашей форме:
    1. Поле ввода (можно набирать текст)
    2. Поле с выбором (нужно выбирать из списка)
    3. Комбинированное (можно и то, и другое)"
```

---

### 3. Проверка КОНТЕКСТА

#### Типы контекста

```yaml
Табличный контекст:
  - "в таблице \"Имя\""
  - "в табличной части \"Имя\""
  
  ✅ Взаимозаменяемы
  ❌ НЕ заменяются на контекст формы

Контекст формы:
  - "в форме"
  - без контекста (подразумевается форма)
  
  ✅ Часто взаимозаменяемы

Контекст дерева:
  - "в дереве \"Имя\""
  
  ❌ Специфичен, не заменяется

Отсутствие контекста:
  - Шаги без указания контекста
  
  ✅ Обычно работают везде
```

#### Примеры проверки контекста

```gherkin
# ✅ ПРАВИЛЬНО - контекст совпадает
Оригинал:    "в таблице \"Товары\" я нажимаю на кнопку"
Рекомендация: "в таблице \"ИмяТаблицы\" я нажимаю на кнопку"
Контекст: в таблице → в таблице ✅
```

```gherkin
# ❌ НЕПРАВИЛЬНО - разный контекст
Оригинал:    "в таблице \"Товары\" я активизирую поле"
Рекомендация: "я активизирую поле"  (контекст формы)
Контекст: в таблице → без контекста (форма) ❌

⚠️ Шаг для таблицы → шаг для формы
→ НЕ применять
```

```gherkin
# ⚠️ ВОЗМОЖНО ОК - нужна проверка
Оригинал:    "я нажимаю кнопку"  (без контекста)
Рекомендация: "в таблице \"Имя\" я нажимаю кнопку"
Контекст: без контекста → в таблице ⚠️

Анализ:
- Добавляется БОЛЕЕ специфичный контекст
- Может не подойти, если кнопка НЕ в таблице

→ Если в исходном сценарии НЕТ упоминания таблицы:
   НЕ применять, обратиться к пользователю
```

---

### 4. Проверка ПАРАМЕТРОВ

#### Типы параметров

```yaml
Заголовок (Caption):
  - "Наименование" (как видит пользователь)
  - "Создать"
  - "Товары"
  
  ✅ Человекочитаемый текст

Имя элемента (Name):
  - "FormCreate" (программное имя)
  - "Номенклатура" (имя реквизита)
  - "Goods" (англ. имя)
  
  ✅ Программный идентификатор

Значение/Данные:
  - "ООО Рога и копыта"
  - "123.45"
  - "01.01.2024"
  
  ✅ Вводимые данные
```

#### Примеры проверки параметров

```gherkin
# ✅ ПРАВИЛЬНО - параметр просто шаблон
Оригинал:    "я нажимаю на кнопку \"Создать\""
Рекомендация: "я нажимаю на кнопку \"ЗаголовокКнопки\""
Параметр: "Создать" → "{}" (шаблон) ✅

→ Применяю с сохранением оригинального параметра:
  "я нажимаю на кнопку \"Создать\""
```

```gherkin
# ⚠️ ОСТОРОЖНО - разные селекторы
Оригинал:    "я нажимаю на кнопку \"Создать\""  (по заголовку)
Рекомендация: "я нажимаю на кнопку с именем \"ИмяКнопки\""  (по имени)

Анализ:
- "по заголовку" ≠ "с именем"
- Это РАЗНЫЕ способы поиска элемента
- Заголовок "Создать" ≠ Имя "FormCreate"

→ НЕ применять автоматически
→ Обратиться к пользователю:
   "Для кнопки нужно использовать:
    1. Заголовок (как видно на экране): \"Создать\"
    2. Программное имя: \"FormCreate\"
    
    Что указано в вашей конфигурации?"
```

```gherkin
# ❌ НЕПРАВИЛЬНО - параметры несовместимы
Оригинал:    "в поле \"Наименование\" я ввожу текст \"Текст\""
Рекомендация: "из выпадающего списка \"Поле\" я выбираю по строке \"Значение\""

Параметры:
- Оригинал: 1 параметр (поле)
- Рекомендация: 2 параметра (поле, значение для поиска)

→ НЕ применять, разная структура параметров
```

---

## 🚨 Антипаттерны (НИКОГДА не делайте так)

### Антипаттерн 1: Слепое применение первой рекомендации

```python
# ❌ НЕПРАВИЛЬНО
if error.suggestions:
    apply_first_suggestion(error.suggestions[0])
```

```python
# ✅ ПРАВИЛЬНО
if error.suggestions:
    for suggestion in error.suggestions:
        analysis = semantic_check(original=error.step, suggested=suggestion)
        if analysis.is_safe and analysis.confidence == "high":
            apply_with_explanation(suggestion, analysis)
            break
    else:
        # Ни одна рекомендация не подошла
        escalate_to_human(error)
```

---

### Антипаттерн 2: Игнорирование счетчика попыток

```python
# ❌ НЕПРАВИЛЬНО - ЗАЦИКЛИВАНИЕ!
while not validated:
    apply_fixes()  # бесконечный цикл
```

```python
# ✅ ПРАВИЛЬНО
MAX_ATTEMPTS = 3
attempt = 0

while not validated and attempt < MAX_ATTEMPTS:
    apply_fixes()
    attempt += 1

if attempt >= MAX_ATTEMPTS:
    escalate_to_human(reason="Max attempts reached")
```

---

### Антипаттерн 3: Применение рекомендации с предупреждениями

```yaml
# Рекомендация от валидатора:
suggestion:
  text: "И я нажимаю на гиперссылку \"Текст\""
  similarity: 0.92
  warnings:
    - "Different UI element type: 'кнопка' vs 'гиперссылка'"
```

```python
# ❌ НЕПРАВИЛЬНО - игнорирование warning
if suggestion.similarity > 0.9:
    apply(suggestion)  # ОШИБКА! Есть warning!
```

```python
# ✅ ПРАВИЛЬНО
if suggestion.similarity > 0.9:
    if suggestion.warnings:
        escalate_to_human(
            error=error,
            suggestion=suggestion,
            reason=f"High similarity but has warnings: {suggestion.warnings}"
        )
    else:
        apply(suggestion)
```

---

## 📊 Таблица принятия решений

| Ситуация | Действие | Confidence | Escalate |
|----------|----------|------------|----------|
| Все проверки ✅ + similarity > 90% | ✅ Применить | High | Нет |
| Все проверки ✅ + similarity 70-90% | ✅ Применить с объяснением | Medium | Нет |
| Элемент ❌ но similarity > 95% | ⚠️ Спросить пользователя | Low | **Да** |
| Действие ❌ | ❌ НЕ применять | None | **Да** |
| Контекст ❌ | ❌ НЕ применять | None | **Да** |
| Есть warnings | ⚠️ Спросить пользователя | Low | **Да** |
| Попытка #3 | - | - | **Да** |
| Нет рекомендаций | - | - | **Да** |

---

## 🎓 Практические примеры

### Пример 1: Идеальный случай

**Ситуация:**
```gherkin
Ошибка: И я нажимаю кнопку "Записать"
```

**Рекомендация:**
```gherkin
И я нажимаю на кнопку "ЗаголовокКнопки"
```

**Семантический анализ:**
```yaml
Действие: нажимаю → нажимаю ✅
Элемент: кнопка → кнопка ✅
Контекст: - → - ✅
Параметр: "Записать" → "{}" ✅ (шаблон)
Similarity: 95%
Warnings: []
```

**Решение:**
```
✅ ПРИМЕНИТЬ с сохранением параметра:
И я нажимаю на кнопку "Записать"

💬 Комментарий для пользователя:
"Добавлен предлог 'на' согласно синтаксису библиотеки Vanessa."
```

---

### Пример 2: Опасная ситуация (разные элементы)

**Ситуация:**
```gherkin
Ошибка: И я нажимаю кнопку "Печать"
```

**Рекомендация #1:**
```gherkin
И я нажимаю на кнопку "Заголовок" (similarity: 94%)
```

**Рекомендация #2:**
```gherkin
И я нажимаю на гиперссылку "Текст" (similarity: 88%)
```

**Семантический анализ #1:**
```yaml
Действие: нажимаю → нажимаю ✅
Элемент: кнопка → кнопка ✅
Similarity: 94%
Warnings: []
Confidence: HIGH
```

**Семантический анализ #2:**
```yaml
Действие: нажимаю → нажимаю ✅
Элемент: кнопка → гиперссылка ❌ РАЗНЫЕ!
Similarity: 88%
Warnings: ["Different UI element type"]
Confidence: LOW
```

**Решение:**
```
✅ Применить рекомендацию #1:
И я нажимаю на кнопку "Печать"

❌ НЕ применять рекомендацию #2:
Причина: Разные типы UI элементов (кнопка vs гиперссылка)
```

---

### Пример 3: Критический случай (зацикливание)

**Попытка #1:**
```gherkin
Ошибка: И я выбираю товар из справочника "Номенклатура"
Рекомендация: И из выпадающего списка "{}" я выбираю по строке "{}"
Применено: И из выпадающего списка "Номенклатура" я выбираю по строке ""
→ Ошибка: второй параметр пустой
```

**Попытка #2:**
```gherkin
Исправление: И из выпадающего списка "Номенклатура" я выбираю по строке "товар"
→ Ошибка: шаг по-прежнему не проходит валидацию
```

**Попытка #3:**
```gherkin
Исправление: И из выпадающего списка "Номенклатура" я выбираю точное значение "товар"
→ Ошибка: другой шаг, та же проблема
```

**Решение:**
```
🚨 MAX_ATTEMPTS (3) достигнут

🤝 ESCALATE TO HUMAN:

"Не удалось автоматически исправить шаг за 3 попытки.

Исходный шаг:
  И я выбираю товар из справочника \"Номенклатура\"

История попыток:
1. Применена рекомендация с выбором из списка - не хватало параметра
2. Добавлен параметр 'товар' - шаг всё равно не валиден
3. Изменен тип выбора - проблема сохранилась

Возможные причины:
- Неправильный подход к выбору из справочника
- Нужен шаг открытия формы выбора элемента
- Требуется использование навигационной ссылки

Рекомендации валидатора:
1. И из выпадающего списка \"{}\" я выбираю по строке \"{}\"
2. И я нажимаю кнопку выбора у поля \"{}\"

Пожалуйста, укажите:
1. Как должен выбираться элемент справочника в вашей конфигурации?
2. Или предоставьте правильный шаг для этого действия"
```

---

## 🛡️ Защита от зацикливания - обязательный код (MAX_ATTEMPTS = 3)

### Шаблон для каждого AI-ассистента

```python
class ValidationLoop:
    MAX_ATTEMPTS = 3
    
    def __init__(self):
        self.attempt = 0
        self.history = []
    
    def validate_and_fix(self, scenario_file):
        while self.attempt < self.MAX_ATTEMPTS:
            result = run_validator(scenario_file)
            
            if result.is_valid:
                return SUCCESS
            
            # Применяем исправления
            applied_fixes = self.apply_semantic_fixes(result.errors)
            
            if not applied_fixes:
                # Не смогли применить ни одного исправления
                self.escalate_to_human(
                    reason="No applicable fixes found",
                    errors=result.errors
                )
                return ESCALATED
            
            self.history.append({
                "attempt": self.attempt,
                "errors": result.errors,
                "fixes": applied_fixes
            })
            
            self.attempt += 1
        
        # Зациклились
        self.escalate_to_human(
            reason="Maximum attempts reached",
            errors=result.errors,
            history=self.history
        )
        return ESCALATED
```

---

## ✅ Чек-лист финальной проверки

Перед отправкой исправленного сценария пользователю:

```
☐ Все автоисправления применены корректно?
☐ Семантическая проверка пройдена для всех замен?
☐ Счетчик попыток не превышен?
☐ Нет ли warnings в примененных рекомендациях?
☐ Все ли параметры заполнены (нет пустых "")?
☐ Сохранена ли логика оригинального сценария?
☐ Добавлены ли комментарии к неочевидным исправлениям?
```

---

## 📞 Шаблоны обращения к пользователю

### Шаблон 1: Неуверенность в выборе

```markdown
⚠️ **Нужна ваша помощь с выбором шага**

**Строка {line}:**
```gherkin
{original_step}
```

**Валидатор предлагает 2 варианта:**

1. `{suggestion_1}` (схожесть: {similarity_1}%)
   - Преимущества: {pros_1}
   - Недостатки: {cons_1}

2. `{suggestion_2}` (схожесть: {similarity_2}%)
   - Преимущества: {pros_2}
   - Недостатки: {cons_2}

**Мой анализ:**
{semantic_analysis}

**Какой вариант использовать?**
[ ] Вариант 1
[ ] Вариант 2
[ ] Другой (укажите)
```

---

### Шаблон 2: Критическое несовпадение

```markdown
🚫 **Обнаружено критическое несовпадение**

**Строка {line}:**
```gherkin
{original_step}
```

**Проблема:**
{problem_description}

**Рекомендация валидатора:**
```gherkin
{suggestion}
```

**Почему НЕ могу применить автоматически:**
- {reason_1}
- {reason_2}

**Ваше решение:**
[ ] Применить рекомендацию (я понимаю риски)
[ ] Исправить шаг вручную
[ ] Использовать другой подход к тестированию
```

---

### Шаблон 3: Зацикливание

```markdown
🔄 **Зациклилсяна исправлении ошибки**

**Строка {line}:**
```gherkin
{original_step}
```

**Попытки исправления ({attempt_count}):**

**Попытка #1:**
- Применено: `{fix_1}`
- Результат: {result_1}

**Попытка #2:**
- Применено: `{fix_2}`
- Результат: {result_2}

**Попытка #3:**
- Применено: `{fix_3}`
- Результат: {result_3}

**Вывод:**
Не могу автоматически исправить эту ошибку.

**Возможные причины:**
{analysis_of_root_cause}

**Рекомендую:**
{recommendations}

**Нужна ваша помощь для продолжения.**
```

---

## 📚 Дополнительные ресурсы

- [`../architecture/ai-validator-integration.md`](../architecture/ai-validator-integration.md) - Общая архитектура
- [`../data/step-categories.yaml`](../data/step-categories.yaml) - Полная категоризация шагов
- [`../examples/semantic-check-examples.md`](../examples/semantic-check-examples.md) - Больше примеров

---

**Помните:** Лучше лишний раз спросить пользователя, чем сломать сценарий!
