# Архитектура: Инструмент сбора контекста форм

> **Цель:** Автоматический сбор минимального, но достаточного описания форм 1С для эффективной генерации сценариев тестирования с помощью AI

---

## 📋 Содержание

1. [Обзор](#обзор)
2. [Архитектура решения](#архитектура-решения)
3. [Компоненты системы](#компоненты-системы)
4. [Форматы данных](#форматы-данных)
5. [Workflow использования](#workflow-использования)
6. [План реализации](#план-реализации)
7. [Примеры использования](#примеры-использования)

---

## Обзор

### Проблема

При написании сценариев тестирования с помощью AI необходим контекст:
- Какие элементы есть на форме?
- Как они называются (имя и заголовок)?
- Какая иерархия элементов?
- Какие действия доступны?

Сейчас эту информацию приходится собирать вручную или использовать встроенный Исследователь форм, который:
- Предназначен для интерактивной работы, а не для AI
- Генерирует избыточную информацию
- Не оптимизирован по токенам
- Не сохраняет результаты для переиспользования

### Решение

**Form Context Collector** - инструмент для автоматического сбора и форматирования информации о формах 1С в AI-оптимизированном виде.

### Ключевые принципы

1. **Минимализм** - только необходимая информация
2. **AI-оптимизация** - структура, понятная для LLM
3. **Переиспользование** - сохранение для последующих запросов
4. **Расширяемость** - возможность добавления деталей
5. **Автоматизация** - минимум ручной работы

---

## Архитектура решения

### Общая схема

```
┌─────────────────┐
│  Пользователь   │
└────────┬────────┘
         │ 1. Запускает сбор контекста
         ▼
┌─────────────────────────────────────────┐
│   Python Script (form_context_cli.py)   │
│  - Парсинг аргументов                   │
│  - Запуск менеджера тестирования        │
└────────┬────────────────────────────────┘
         │ 2. Запускает VA + TestClient
         ▼
┌─────────────────────────────────────────┐
│  Vanessa Automation Manager             │
│  + TestClient (открыта целевая форма)   │
└────────┬────────────────────────────────┘
         │ 3. Вызывает обработку
         ▼
┌─────────────────────────────────────────┐
│  Обработка 1С                           │
│  (FormContextCollector.epf)             │
│  - Сканирует открытую форму             │
│  - Собирает минимальный набор свойств   │
│  - Экспортирует в JSON                  │
└────────┬────────────────────────────────┘
         │ 4. Сохраняет сырые данные
         ▼
┌─────────────────────────────────────────┐
│  Python Formatter                       │
│  (form_context_formatter.py)            │
│  - Загружает JSON                       │
│  - Создает Markdown (AI-оптимизация)    │
│  - Создает screenshots (опционально)    │
└────────┬────────────────────────────────┘
         │ 5. Сохраняет результаты
         ▼
┌─────────────────────────────────────────┐
│  Директория context/                    │
│  ├── forms/                             │
│  │   ├── Документ.ЗаказПокупателя.json │
│  │   ├── Документ.ЗаказПокупателя.md   │
│  │   └── Документ.ЗаказПокупателя.png  │
│  └── index.json (реестр форм)           │
└─────────────────────────────────────────┘
```

---

## Компоненты системы

### 1. Python CLI (form_context_cli.py)

**Назначение:** Точка входа, управление процессом

**Функции:**
- Парсинг аргументов командной строки
- Запуск Vanessa Automation с TestClient
- Координация работы компонентов
- Обработка ошибок

**Интерфейс:**
```bash
# Базовое использование
python tools/form-context/form_context_cli.py \
    --form "e1cib/data/Документ.ЗаказПокупателя" \
    --output context/forms/

# С дополнительными опциями
python tools/form-context/form_context_cli.py \
    --form "e1cib/data/Документ.ЗаказПокупателя" \
    --detail-level full \
    --screenshot \
    --output context/forms/
```

**Аргументы:**
- `--form` - навигационная ссылка или путь к форме
- `--output` - директория для сохранения (по умолчанию: `context/forms/`)
- `--detail-level` - уровень детализации: `minimal`, `medium`, `full` (по умолчанию: `minimal`)
- `--screenshot` - создать скриншот формы (опционально)
- `--format` - форматы вывода: `json`, `markdown`, `both` (по умолчанию: `both`)

### 2. Обработка 1С (FormContextCollector.epf)

**Назначение:** Сбор данных о форме из TestClient

**Основано на:** `ИсследовательФормы` из Vanessa Automation

**Ключевые отличия от Исследователя:**
- Минимальный набор свойств по умолчанию
- Настраиваемая детализация
- Прямой экспорт в JSON
- Оптимизация по производительности

**Структура обработки:**

```
FormContextCollector/
├── Форма
│   ├── ФормаСбораКонтекста (минималистичная UI)
│   └── Модуль формы
├── МодульОбъекта (основная логика)
└── Макеты
    └── ШаблонJSON
```

**Минимальный набор свойств элемента:**
```json
{
  "Имя": "string",           // Имя элемента (для шагов с 'именем')
  "Заголовок": "string",     // Заголовок (для шагов с текстом)
  "Тип": "string",           // ТестируемоеПолеФормы, ТестируемаяКнопкаФормы, etc.
  "Вид": "string",           // ВидПоляФормы.ПолеВвода, ВидКнопкиФормы, etc.
  "Родитель": "string"       // Имя родительского элемента (для иерархии)
}
```

**Дополнительные свойства (medium/full):**
```json
{
  "ТекущаяВидимость": "boolean",
  "ТекущаяДоступность": "boolean",
  "ПутьКДанным": "string",
  "ИмяКоманды": "string"     // для кнопок
}
```

**Алгоритм сбора:**
```bsl
Функция СобратьКонтекстФормы(УровеньДетализации = "minimal") Экспорт
    // 1. Получить активное окно и форму из TestClient
    ТекущееОкно = ПолучитьАктивноеОкноИзTestClient();
    ТекущаяФорма = ПолучитьФормуИзОкна(ТекущееОкно);
    
    // 2. Создать корневую структуру
    Результат = Новый Структура;
    Результат.Вставить("ИмяФормы", ТекущаяФорма.ИмяФормы);
    Результат.Вставить("Заголовок", ТекущаяФорма.ТекстЗаголовка);
    Результат.Вставить("Элементы", Новый Массив);
    
    // 3. Сканировать элементы (рекурсивно или линейно)
    Если УровеньДетализации = "minimal" Тогда
        // Линейный обход (быстрее, без иерархии)
        СобратьЭлементыЛинейно(ТекущаяФорма, Результат.Элементы);
    Иначе
        // Рекурсивный обход (иерархия)
        СобратьЭлементыРекурсивно(ТекущаяФорма, Результат.Элементы);
    КонецЕсли;
    
    // 4. Экспортировать в JSON
    Возврат ЗаписатьJSONСтрокой(Результат);
КонецФункции
```

### 3. Python Formatter (form_context_formatter.py)

**Назначение:** Преобразование сырых данных в AI-оптимизированный формат

**Функции:**
- Загрузка JSON от обработки 1С
- Создание Markdown с оптимизацией для AI
- Генерация индекса форм
- Создание скриншотов (если требуется)

**Оптимизации для AI:**

1. **Группировка по типам элементов**
```markdown
## Поля ввода
- `Организация` ("Организация") - выбор из справочника
- `Дата` ("Дата") - поле даты
- `Номер` ("Номер") - текстовое поле

## Таблицы
- `Товары` ("Товары и услуги")
  - `ТоварыНоменклатура` ("Номенклатура")
  - `ТоварыКоличество` ("Количество")
  - `ТоварыЦена` ("Цена")

## Кнопки
- `ФормЗаписать` ("Записать")
- `ФормПровести` ("Провести")
```

2. **Сокращенный синтаксис**
```markdown
Формат: `Имя` ("Заголовок") - краткое описание
```

3. **Удаление избыточности**
- Не дублируем очевидную информацию
- Группируем однотипные элементы
- Опускаем технические детали (если не требуется)

### 4. Хранилище контекста (context/)

**Структура директорий:**
```
context/
├── forms/                           # Описания форм
│   ├── Справочник.Контрагенты.json
│   ├── Справочник.Контрагенты.md
│   ├── Справочник.Контрагенты.png   # опционально
│   ├── Документ.ЗаказПокупателя.json
│   ├── Документ.ЗаказПокупателя.md
│   └── ...
├── index.json                       # Реестр всех форм
└── README.md                        # Описание структуры
```

**Формат index.json:**
```json
{
  "version": "1.0",
  "updated_at": "2025-11-29T10:00:00Z",
  "forms": {
    "Справочник.Контрагенты": {
      "title": "Контрагент (форма элемента)",
      "json_path": "forms/Справочник.Контрагенты.json",
      "markdown_path": "forms/Справочник.Контрагенты.md",
      "screenshot_path": "forms/Справочник.Контрагенты.png",
      "collected_at": "2025-11-29T09:30:00Z",
      "element_count": 45,
      "detail_level": "minimal"
    }
  }
}
```

---

## Форматы данных

### JSON (сырые данные от 1С)

**Файл:** `Документ.ЗаказПокупателя.json`

```json
{
  "version": "1.0",
  "collected_at": "2025-11-29T10:00:00Z",
  "form": {
    "name": "Документ.ЗаказПокупателя.Форма.ФормаДокумента",
    "title": "Заказ покупателя",
    "type": "DocumentForm"
  },
  "elements": [
    {
      "name": "Организация",
      "title": "Организация",
      "type": "TestableFormField",
      "kind": "InputField",
      "parent": null,
      "path": "Объект.Организация"
    },
    {
      "name": "Дата",
      "title": "от",
      "type": "TestableFormField",
      "kind": "InputField",
      "parent": null,
      "path": "Объект.Дата"
    },
    {
      "name": "Товары",
      "title": "Товары и услуги",
      "type": "TestableFormTable",
      "kind": "Table",
      "parent": null,
      "columns": [
        {
          "name": "ТоварыНоменклатура",
          "title": "Номенклатура",
          "type": "TestableFormField",
          "kind": "InputField"
        },
        {
          "name": "ТоварыКоличество",
          "title": "Количество",
          "type": "TestableFormField",
          "kind": "InputField"
        }
      ]
    },
    {
      "name": "ФормаЗаписать",
      "title": "Записать",
      "type": "TestableFormButton",
      "kind": "Button",
      "command": "FormWrite"
    }
  ],
  "statistics": {
    "total_elements": 25,
    "fields": 10,
    "tables": 1,
    "buttons": 8,
    "groups": 6
  }
}
```

### Markdown (AI-оптимизированный)

**Файл:** `Документ.ЗаказПокупателя.md`

```markdown
# Форма: Заказ покупателя

**Тип:** Документ.ЗаказПокупателя  
**Собрано:** 2025-11-29 10:00

---

## 📝 Основные реквизиты

- `Организация` ("Организация") - организация
- `Дата` ("от") - дата документа
- `Номер` ("№") - номер документа
- `Контрагент` ("Контрагент") - покупатель
- `Договор` ("Договор") - договор с покупателем
- `Склад` ("Склад") - склад отгрузки

## 📊 Табличные части

### Товары (`Товары`)
Заголовок: "Товары и услуги"

**Колонки:**
- `ТоварыНоменклатура` ("Номенклатура") - товар/услуга
- `ТоварыКоличество` ("Количество") - количество
- `ТоварыЦена` ("Цена") - цена за единицу
- `ТоварыСумма` ("Сумма") - сумма строки (только чтение)

## 🔘 Команды

- `ФормаЗаписать` ("Записать") - записать документ
- `ФормаПровести` ("Провести") - провести документ
- `ФормаПровестиИЗакрыть` ("Провести и закрыть")
- `ТоварыДобавить` ("Добавить") - добавить строку в таблицу
- `ТоварыУдалить` ("Удалить") - удалить строку

---

**Всего элементов:** 25 (10 полей, 1 таблица, 8 кнопок, 6 групп)
```

### Компактный формат (для промптов)

**Для использования в промптах к AI:**

```markdown
## Форма: Заказ покупателя (Документ.ЗаказПокупателя)

Поля: `Организация`, `Дата`, `Номер`, `Контрагент`, `Договор`, `Склад`

Таблица `Товары`: `ТоварыНоменклатура`, `ТоварыКоличество`, `ТоварыЦена`, `ТоварыСумма`

Кнопки: `ФормаЗаписать` ("Записать"), `ФормаПровести` ("Провести"), `ТоварыДобавить` ("Добавить")
```

---

## Workflow использования

### Сценарий 1: Ручной сбор контекста

```bash
# 1. Открыть форму в TestClient вручную
# (пользователь открывает нужную форму в клиенте тестирования)

# 2. Запустить сбор контекста
python tools/form-context/form_context_cli.py \
    --form "Документ.ЗаказПокупателя" \
    --output context/forms/

# 3. Результат: созданы файлы
# context/forms/Документ.ЗаказПокупателя.json
# context/forms/Документ.ЗаказПокупателя.md

# 4. Использовать в промпте
# "Создай сценарий для формы, описание которой в context/forms/Документ.ЗаказПокупателя.md"
```

### Сценарий 2: Автоматический сбор (будущее)

```bash
# Полностью автоматический процесс
python tools/form-context/form_context_cli.py \
    --auto \
    --form "e1cib/data/Документ.ЗаказПокупателя?ref=..." \
    --va-path "C:/VanessaAutomation/" \
    --infobase "File=C:/Bases/Test/"

# Скрипт:
# 1. Запускает Vanessa Automation
# 2. Подключается к TestClient
# 3. Открывает форму по навигационной ссылке
# 4. Собирает контекст
# 5. Закрывает всё
```

### Сценарий 3: Интеграция с AI workflow

```python
# В коде AI-агента (например, в Kilo Code)
from tools.form_context import FormContextProvider

# Получить контекст формы
context = FormContextProvider.get_context("Документ.ЗаказПокупателя")

# Использовать в промпте
prompt = f"""
Создай сценарий для формы:

{context.to_compact_markdown()}

Требования: ...
"""
```

---

## План реализации

### Фаза 1: MVP (Минимальная функциональность)

**Цель:** Базовая ручная работа

**Задачи:**
1. ✅ Спроектировать архитектуру
2. ⬜ Создать обработку 1С (FormContextCollector.epf)
   - Базовый сбор элементов (линейный обход)
   - Экспорт в JSON с минимальными свойствами
   - Форма для запуска из VA
3. ⬜ Создать Python formatter
   - Парсинг JSON от 1С
   - Генерация Markdown (AI-оптимизация)
   - Создание index.json
4. ⬜ Создать CLI (form_context_cli.py)
   - Базовый запуск formatter'а
   - Обработка аргументов
5. ⬜ Тестирование на реальных формах
6. ⬜ Документация

**Результат:** Работающий инструмент для ручного сбора контекста

### Фаза 2: Улучшения

**Цель:** Удобство использования

**Задачи:**
1. ⬜ Добавить уровни детализации (minimal/medium/full)
2. ⬜ Реализовать иерархический обход (для групп)
3. ⬜ Добавить фильтрацию элементов
4. ⬜ Кэширование и переиспользование
5. ⬜ Batch-режим (несколько форм за раз)

### Фаза 3: Автоматизация

**Цель:** Полная автоматизация

**Задачи:**
1. ⬜ Интеграция с запуском VA
2. ⬜ Автоматическое открытие форм по навигационным ссылкам
3. ⬜ Поддержка скриншотов
4. ⬜ API для программного доступа

### Фаза 4: Расширенные возможности

**Цель:** Продвинутые фичи

**Задачи:**
1. ⬜ Diff между версиями формы
2. ⬜ Автоматическая генерация примеров шагов
3. ⬜ Интеграция с валидатором
4. ⬜ Web UI для просмотра контекста
5. ⬜ Экспорт в другие форматы (YAML, XML)

---

## Примеры использования

### Пример 1: Простой справочник

**Команда:**
```bash
python tools/form-context/form_context_cli.py \
    --form "Справочник.Контрагенты" \
    --detail-level minimal
```

**Результат (Markdown):**
```markdown
# Форма: Контрагент

## Поля
- `Наименование` ("Наименование")
- `ИНН` ("ИНН")
- `КПП` ("КПП")
- `ЮрАдрес` ("Юридический адрес")

## Кнопки
- `ФормаЗаписать` ("Записать и закрыть")
```

### Пример 2: Сложный документ

**Команда:**
```bash
python tools/form-context/form_context_cli.py \
    --form "Документ.РеализацияТоваровУслуг" \
    --detail-level medium \
    --screenshot
```

**Результат:**
- `Документ.РеализацияТоваровУслуг.json` (полные данные)
- `Документ.РеализацияТоваровУслуг.md` (читаемое описание)
- `Документ.РеализацияТоваровУслуг.png` (скриншот формы)

### Пример 3: Использование в AI промпте

**Промпт:**
```
Создай сценарий создания документа "Заказ покупателя".

Контекст формы:
[вставить содержимое Документ.ЗаказПокупателя.md]

Требования:
- Заполнить все обязательные поля
- Добавить 2 строки в табличную часть
- Провести документ
- Проверить что документ проведен
```

---

## Технические детали

### Зависимости

**1С (Обработка):**
- Vanessa Automation (для доступа к TestClient)
- Платформа 8.3.10+

**Python:**
- Python 3.6+
- Только стандартная библиотека (json, pathlib, datetime)

### Производительность

**Ожидаемое время:**
- Сбор контекста простой формы: < 1 сек
- Сбор контекста сложной формы: 2-3 сек
- Форматирование: < 0.5 сек
- **Итого:** 1-4 секунды на форму

### Ограничения

1. **Требуется открытая форма в TestClient** (для MVP)
2. **Не работает с модальными окнами** (пока)
3. **Не собирает динамические элементы** (создаваемые программно)
4. **Скриншоты только для тонкого клиента** (ограничение TestClient)

---

## Взаимодействие с va-ai

### Интеграция в структуру проекта

```
va-ai/
├── tools/
│   ├── form-context/              # ← НОВЫЙ ИНСТРУМЕНТ
│   │   ├── README.md
│   │   ├── form_context_cli.py
│   │   ├── form_context_formatter.py
│   │   ├── form_context_provider.py
│   │   └── 1c/
│   │       └── FormContextCollector.epf
│   ├── validator/
│   └── search-steps/
├── context/                       # ← НОВАЯ ДИРЕКТОРИЯ
│   ├── forms/
│   ├── index.json
│   └── README.md
└── ...
```

### Workflow с другими инструментами

```
1. Form Context Collector → собирает контекст формы
                              ↓
2. AI (with context)        → генерирует сценарий
                              ↓
3. Validator                → проверяет сценарий
                              ↓
4. AI (with errors)         → исправляет ошибки
                              ↓
5. ✓ Готовый сценарий
```

---

## Дальнейшее развитие

### Идеи для будущих версий

1. **Умное кэширование**
   - Автоматическое обновление при изменении формы
   - Версионирование описаний

2. **AI-генерация примеров**
   - Автоматическая генерация типовых шагов для формы
   - Предзаполненные шаблоны сценариев

3. **Интеграция с IDE**
   - VS Code extension
   - Kilo Code integration
   - Cursor.ai support

4. **Веб-интерфейс**
   - Просмотр всех собранных форм
   - Поиск по элементам
   - Экспорт в разных форматах

5. **Аналитика**
   - Статистика использования элементов в сценариях
   - Рекомендации по улучшению тестов

---

## Глоссарий

- **Form Context** - набор информации о форме, достаточный для написания сценариев
- **Detail Level** - уровень детализации собираемых данных
- **TestClient** - компонент VA для управления тестовым клиентом 1С
- **AI-оптимизация** - структурирование данных для эффективного восприятия LLM
- **Компактный формат** - максимально сжатое представление для экономии токенов

---

**Версия документа:** 1.0  
**Дата создания:** 2025-11-29  
**Автор:** Nikolay Shirokov