# Best Practices: AI-ассистент + Валидатор Vanessa Automation

> **Версия:** 1.0  
> **Дата:** 2025-11-28  
> **Аудитория:** Разработчики, внедряющие AI-валидацию

---

## 🎯 Введение

Этот документ содержит проверенные практики для эффективной работы AI-ассистента с валидатором сценариев Vanessa Automation, включая предотвращение зацикливания и правильное взаимодействие с пользователем.

---

## 📋 Общие принципы

### Принцип 1: Human-in-the-Loop превыше всего

**Правило:** Лучше лишний раз спросить пользователя, чем сломать сценарий.

```python
# ✅ ПРАВИЛЬНО
if confidence < 0.8 or has_warnings:
    ask_user_for_confirmation()
else:
    apply_with_explanation()
```

```python
# ❌ НЕПРАВИЛЬНО
if similarity > 0.7:
    apply_directly()  # Риск! Высокая схожесть ≠ семантическая релевантность
```

---

### Принцип 2: Защита от зацикливания обязательна

**Правило:** Максимум 3 попытки исправления. Далее - к пользователю.

```python
# ✅ ПРАВИЛЬНО
MAX_ATTEMPTS = 3
attempt = 0

while not validated and attempt < MAX_ATTEMPTS:
    apply_fixes()
    attempt += 1

if attempt >= MAX_ATTEMPTS:
    escalate_to_human(history=all_attempts)
```

**Почему именно 3?**
- 1 попытка: Очевидное исправление
- 2 попытка: Альтернативный подход
- 3 попытка: Последний шанс
- После 3: Явно проблема глубже → нужен пользователь

---

### Принцип 3: Семантическая проверка перед каждым применением

**Правило:** Проверять ВСЕ 4 критерия для каждой рекомендации.

```python
# ✅ ПРАВИЛЬНО
def validate_suggestion(original, suggested):
    checks = {
        'action': compare_actions(original.action, suggested.action),
        'element': compare_elements(original.element, suggested.element),
        'context': compare_context(original.context, suggested.context),
        'params': compare_params(original.params, suggested.params)
    }
    
    # ВСЕ критичные должны совпадать
    if checks['action'] < 0.9 or checks['element'] < 0.9:
        return REJECT  # Критичное несовпадение
    
    # Вычисляем общую уверенность
    confidence = calculate_weighted_confidence(checks)
    
    return {
        'decision': APPLY if confidence > 0.8 else ASK_USER,
        'confidence': confidence,
        'checks': checks
    }
```

---

### Принцип 4: Прозрачность для пользователя

**Правило:** Объяснять каждое исправление и причины обращения.

```markdown
# ✅ ПРАВИЛЬНО
✅ Исправлен шаг на строке 8:
   Было: И я нажимаю кнопку "Создать"
   Стало: И я нажимаю на кнопку "Создать"
   
💬 Причина: Добавлен предлог "на" согласно синтаксису библиотеки Vanessa.
   Действие и тип элемента совпадают (confidence: 95%).
```

```markdown
# ❌ НЕПРАВИЛЬНО
Исправлено.  # Что? Как? Почему?
```

---

## 🛠️ Практики по уровням ошибок

### Уровень 1: Auto-Fix (Автоисправление)

**Что исправлять автоматически:**
- ✅ Синтаксис кавычек (`'` → `"`)
- ✅ Отсутствующие заголовки (`# encoding: utf-8`)
- ✅ Отсутствующий блок `Функционал:`
- ✅ Очевидные опечатки в ключевых словах

**Как:**
```python
def handle_auto_fix_errors(errors):
    for error in errors:
        if error.type == "quotes":
            fix_quotes(error)
        elif error.type == "encoding":
            add_encoding_header()
        elif error.type == "feature_block":
            add_feature_block()
        
        # Логировать каждое исправление
        log_fix(error, auto=True)
    
    # Повторная валидация обязательна
    revalidate()
```

**Best Practice:**
- Группировать auto-fix ошибки и исправлять все за один проход
- Всегда сообщать пользователю что было исправлено
- Повторная валидация после каждой группы исправлений

---

### Уровень 2: Semantic Check (Семантическая проверка)

**Алгоритм принятия решения:**

```python
def handle_semantic_errors(error):
    """Обработка ошибок требующих семантической проверки"""
    
    if not error.suggestions:
        # Нет рекомендаций → к пользователю
        escalate_to_human(error, reason="No suggestions")
        return
    
    # Анализируем каждую рекомендацию
    analysis_results = []
    for suggestion in error.suggestions:
        analysis = analyze_semantic_match(
            original=error.step,
            suggested=suggestion.text,
            metadata=suggestion.parsed
        )
        analysis_results.append(analysis)
    
    # Сортируем по безопасности и уверенности
    analysis_results.sort(
        key=lambda x: (x.is_safe, x.confidence),
        reverse=True
    )
    
    best_match = analysis_results[0]
    
    # Принимаем решение
    if best_match.is_safe and best_match.confidence > 0.8:
        # Высокая уверенность → применить
        apply_with_explanation(best_match)
        
    elif best_match.confidence > 0.6:
        # Средняя уверенность → спросить
        ask_user_choice(error, analysis_results[:3])
        
    else:
        # Низкая уверенность → к пользователю
        escalate_to_human(
            error, 
            analysis=best_match,
            reason="Low confidence in all suggestions"
        )
```

**Best Practice:**
- Анализировать все рекомендации, не только первую
- Сортировать по безопасности (is_safe), затем по уверенности
- Показывать пользователю топ-3 лучших варианта при запросе

---

### Уровень 3: Critical (Критические)

**Когда эскалировать к пользователю немедленно:**

```python
def is_critical_error(error):
    """Определение критичности ошибки"""
    return (
        # Нет рекомендаций
        len(error.suggestions) == 0
        or
        # Все рекомендации с критичными warnings
        all(s.has_critical_warning for s in error.suggestions)
        or
        # Попытка #3 и выше
        attempt_counter >= MAX_ATTEMPTS
        or
        # AI явно не уверен
        all(s.confidence < 0.5 for s in error.suggestions)
    )
```

**Best Practice:**
- Предоставлять максимум контекста при эскалации
- Показывать историю попыток если были
- Предлагать конкретные варианты действий
- Не пытаться "угадать" решение

---

## 🎨 Паттерны взаимодействия с пользователем

### Паттерн 1: Запрос выбора из вариантов

**Когда использовать:** Несколько равнозначных вариантов с confidence > 0.6

```markdown
⚠️ Нужна ваша помощь с выбором шага

**Строка 8:** `И я нажимаю кнопку "Печать"`

Валидатор предлагает 2 подходящих варианта:

**Вариант 1:** (рекомендую)
```gherkin
И я нажимаю на кнопку "Печать"
```
✅ Преимущества: Точное совпадение действия и элемента
⚠️ Изменения: Добавлен предлог "на"
📊 Уверенность: 95%

**Вариант 2:**
```gherkin
И я нажимаю на кнопку с именем "FormPrint"
```
✅ Преимущества: Более надежный селектор (по имени)
⚠️ Изменения: Нужно указать программное имя кнопки
📊 Уверенность: 85%

**Какой вариант использовать?**
[ ] Вариант 1 (по заголовку)
[ ] Вариант 2 (по имени - укажите имя кнопки)
```

---

### Паттерн 2: Предупреждение о потенциальных проблемах

**Когда использовать:** Применили исправление, но есть нюансы

```markdown
✅ Шаг исправлен

**Было:**
```gherkin
И в поле "Номенклатура" я ввожу текст "Товар 1"
```

**Стало:**
```gherkin
И в поле "Номенклатура" я ввожу текст "Товар 1"
```

💬 **Обратите внимание:**
Поле "Номенклатура" исправлено как поле ввода текста.

Если в вашей конфигурации это поле с выбором из справочника, 
лучше использовать:
```gherkin
И из выпадающего списка "Номенклатура" я выбираю по строке "Товар 1"
```

**Текущий вариант подходит?** [✅ Да / ⚠️ Нужно изменить]
```

---

### Паттерн 3: Эскалация с полным контекстом

**Когда использовать:** Зацикливание или невозможность автоисправления

```markdown
🔄 **Зациклился на исправлении ошибки**

Не удалось автоматически исправить за 3 попытки.

---

**Исходный шаг (строка 12):**
```gherkin
И я выбираю товар из справочника "Номенклатура"
```

---

**📊 История попыток:**

**Попытка #1:** (similarity: 82%)
Применено: `И из выпадающего списка "Номенклатура" я выбираю по строке ""`
Результат: ❌ Второй параметр не заполнен
Причина: Валидатор требует 2 параметра, заполнен только 1

**Попытка #2:** (similarity: 82%)
Применено: `И из выпадающего списка "Номенклатура" я выбираю по строке "товар"`
Результат: ❌ Шаг не проходит валидацию
Причина: Контекст некорректен для этого типа поля

**Попытка #3:** (similarity: 75%)
Применено: `И я нажимаю кнопку выбора у поля "Номенклатура"`
Результат: ❌ Неполная последовательность
Причина: Шаг корректен, но требует продолжения (выбор в открывшейся форме)

---

**🔍 Мой анализ:**

Выбор элемента из справочника - сложная операция, требующая:
1. Открытие формы выбора
2. Поиск/фильтрация
3. Выбор строки
4. Подтверждение

Одного шага недостаточно.

---

**💡 Рекомендуемые решения:**

**Вариант 1: Полная последовательность шагов**
```gherkin
И я нажимаю кнопку выбора у поля "Номенклатура"
Тогда открылось окно "Номенклатура"
И в таблице "Список" я перехожу к строке с условием 'Наименование = "Товар 1"'
И в таблице "Список" я выбираю текущую строку
```
✅ Подробно, надежно
⚠️ Многословно

**Вариант 2: Прямой ввод (если поддерживается конфигурацией)**
```gherkin
И в поле "Номенклатура" я ввожу текст "Товар 1"
```
✅ Просто, быстро
⚠️ Работает только если поле поддерживает ввод без выбора

**Вариант 3: Через переменную с заранее известной ссылкой**
```gherkin
# В контексте или в начале сценария
И я запоминаю значение выражения 'ПолучитьНавигационнуюСсылку(Справочники.Номенклатура.НайтиПоНаименованию("Товар 1"))' в переменную "СсылкаНаТовар"
# В нужном месте
И в поле "Номенклатура" я ввожу текст "$СсылкаНаТовар$"
```
✅ Надежно
⚠️ Требует подготовки данных

---

**❓ Ваше решение:**
[ ] Вариант 1 (полная последовательность)
[ ] Вариант 2 (прямой ввод)
[ ] Вариант 3 (через переменную)
[ ] Другой вариант (опишите)
```

---

## 🔒 Правила безопасности

### Правило 1: Критичные несовпадения = СТОП

**Никогда не заменять:**

```yaml
Действие:
  ввожу → выбираю  # ❌ СТОП! Разная логика
  нажимаю → проверяю  # ❌ СТОП! Разная категория

Элемент:
  кнопка → гиперссылка  # ❌ СТОП! Разные UI элементы
  поле → выпадающий список  # ❌ СТОП! Разные элементы
  таблица → форма  # ❌ СТОП! Разный контекст
```

**Код проверки:**

```python
CRITICAL_MISMATCHES = {
    'action': [
        ('ввожу', 'выбираю'),
        ('нажимаю', 'проверяю'),
        ('открываю', 'закрываю'),
    ],
    'element': [
        ('кнопка', 'гиперссылка'),
        ('поле', 'список'),
        ('таблица', 'форма'),
    ]
}

def is_critical_mismatch(original, suggested):
    for category, mismatches in CRITICAL_MISMATCHES.items():
        orig_val = getattr(original, category)
        sugg_val = getattr(suggested, category)
        
        if (orig_val, sugg_val) in mismatches:
            return True, f"Critical mismatch in {category}: {orig_val} → {sugg_val}"
    
    return False, None
```

---

### Правило 2: Warnings = Осторожность

**Если suggestion содержит warnings:**

```python
def handle_warnings(suggestion):
    if not suggestion.warnings:
        return SAFE_TO_APPLY
    
    critical_warnings = [
        "Different UI element type",
        "Different action type",
        "Context mismatch"
    ]
    
    has_critical = any(
        warning in critical_warnings 
        for warning in suggestion.warnings
    )
    
    if has_critical:
        return ASK_USER  # Обязательно спросить
    else:
        return EXPLAIN_AND_APPLY  # Применить с объяснением
```

---

### Правило 3: Rollback при проблемах

**Сохранять историю для возможности отката:**

```python
class ValidationSession:
    def __init__(self):
        self.history = []
        self.snapshots = []
    
    def apply_fix(self, fix):
        # Сохранить состояние перед изменением
        self.snapshots.append(self.get_current_state())
        
        # Применить
        result = self._apply(fix)
        
        # Сохранить в историю
        self.history.append({
            'fix': fix,
            'result': result,
            'timestamp': now()
        })
        
        return result
    
    def rollback(self, steps=1):
        """Откат на N шагов назад"""
        if len(self.snapshots) >= steps:
            state = self.snapshots[-(steps+1)]
            self.restore_state(state)
```

---

## 📈 Метрики качества

### KPI для AI-валидатора

```yaml
Целевые показатели:
  auto_fix_success_rate: 100%  # Автоисправления всегда корректны
  semantic_check_accuracy: >95%  # Правильность семантических проверок
  false_positive_escalations: <10%  # Ненужных обращений к пользователю
  infinite_loop_incidents: 0%  # Случаев зацикливания
  
Производительность:
  avg_validation_time: <5 сек
  avg_fix_application_time: <3 сек
  max_attempts_before_escalation: 3
  
Пользовательский опыт:
  user_satisfaction: >90%
  clear_explanations: 100%  # Всегда объяснять
  actionable_escalations: 100%  # Всегда давать варианты
```

### Логирование для анализа

```python
def log_validation_event(event_type, details):
    """Структурированное логирование для последующего анализа"""
    
    log_entry = {
        'timestamp': now(),
        'event_type': event_type,  # auto_fix / semantic_check / escalation
        'scenario_file': details.get('file'),
        'line_number': details.get('line'),
        'original_step': details.get('original'),
        'suggestion': details.get('suggestion'),
        'confidence': details.get('confidence'),
        'decision': details.get('decision'),  # apply / reject / ask_user
        'reason': details.get('reason'),
        'attempt_number': details.get('attempt', 1)
    }
    
    # Сохранить для анализа
    save_to_analytics(log_entry)
```

---

## 🎓 Обучение и улучшение

### Сбор обратной связи

```python
def collect_feedback(validation_result):
    """После успешной валидации - собрать фидбек"""
    
    if validation_result.had_fixes:
        feedback = ask_user({
            'question': 'Исправления были корректны?',
            'options': [
                '✅ Да, все правильно',
                '⚠️ Частично (укажите что не так)',
                '❌ Нет, исправления некорректны'
            ]
        })
        
        if feedback != '✅ Да, все правильно':
            save_feedback_for_training({
                'scenario': validation_result.scenario,
                'fixes_applied': validation_result.fixes,
                'user_feedback': feedback,
                'timestamp': now()
            })
```

### Обновление правил на основе опыта

```python
def analyze_feedback_and_improve():
    """Периодический анализ накопленного фидбека"""
    
    feedback_data = load_feedback_history()
    
    # Найти паттерны ошибок
    error_patterns = find_common_error_patterns(feedback_data)
    
    # Обновить правила
    for pattern in error_patterns:
        if pattern.frequency > THRESHOLD:
            update_validation_rules(pattern)
            notify_admin(f"Добавлено новое правило на основе {pattern.frequency} случаев")
```

---

## 🔗 Чек-лист финальной проверки

Перед отправкой результата пользователю:

```
☐ Валидация пройдена успешно?
☐ Счетчик попыток не превышен (<= 3)?
☐ Все автоисправления логированы?
☐ Семантическая проверка пройдена для всех замен?
☐ Нет критичных warnings в применённых рекомендациях?
☐ Добавлены объяснения ко всем изменениям?
☐ Логика сценария сохранена?
☐ Все параметры заполнены корректно?
☐ Пользователь информирован о всех изменениях?
☐ История сохранена для анализа?
```

---

## 📚 Связанные документы

- [`architecture/ai-validator-integration.md`](architecture/ai-validator-integration.md) - Архитектура решения
- [`../ai-knowledge/semantic-validation-guide.md`](../ai-knowledge/semantic-validation-guide.md) - Правила семантической проверки
- [`../templates/prompts/ai-validator-workflow.md`](../templates/prompts/ai-validator-workflow.md) - Промпты для AI
- [`../examples/validation-testing/test-cases-for-ai.md`](../examples/validation-testing/test-cases-for-ai.md) - Тестовые кейсы

---

**Помните:** Качество автоматизации определяется не количеством автоматических исправлений, а правильностью принятых решений! 🎯
