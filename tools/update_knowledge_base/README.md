# Обновление базы знаний

## 🔄 Скрипт update_knowledge_base.py

Автоматическое обновление базы знаний AI из новой версии библиотеки шагов Vanessa Automation.

---

## 🎯 Что делает скрипт

1. ✅ **Загружает** новую библиотеку шагов из Vanessa Automation
2. ✅ **Оптимизирует** структуру для AI (группировка по категориям)
3. ✅ **Сравнивает** с предыдущей версией (новые/удаленные шаги)
4. ✅ **Генерирует** статистику
5. ✅ **Обновляет** все файлы:
   - `ai-knowledge/steps-library.json` - база для AI
   - `ai-knowledge/README.md` - инструкция
   - `data/library-full.json` - полная библиотека
   - `data/statistics.json` - статистика
   - `data/README.md` - описание данных

---

## 🚀 Быстрый старт

### 1. Поместите `БиблиотекаШагов.json` в папку `upload`

Экспортируйте библиотеку из Vanessa Automation и сохраните ее под именем `БиблиотекаШагов.json` в директорию `upload`, которая находится в корне проекта.

### 2. Запустите скрипт

Откройте терминал в корне проекта и выполните:
```bash
python tools/update_knowledge_base/update_knowledge_base.py
```
Скрипт автоматически найдет файл и обновит все необходимые данные.

### 3. Готово!

Все файлы обновлены автоматически.

---

## 📖 Использование

### Базовое использование

```bash
# По умолчанию, скрипт ищет файл в upload/БиблиотекаШагов.json
python tools/update_knowledge_base/update_knowledge_base.py

# Можно также указать путь к файлу явно
python tools/update_knowledge_base/update_knowledge_base.py path/to/your/library.json
```

### Предпросмотр (без изменений)

```bash
python update_knowledge_base.py БиблиотекаШагов.json --dry-run
```

Покажет что будет сделано, но не применит изменения.

### Указание директории

```bash
python update_knowledge_base.py БиблиотекаШагов.json --output-dir ai-knowledge/
```

### Полный пример

```bash
python update_knowledge_base.py /path/to/БиблиотекаШагов.json \
    --output-dir ai-knowledge/ \
    --dry-run
```

---

## 📊 Что показывает скрипт

### Информация о загрузке

```
✓ Загрузка библиотеки из БиблиотекаШагов.json...
✅ Загружено 1569 шагов
✓ Загружена старая база: 1500 шагов

Создание базы знаний для AI...
```

### Сравнение версий

```
Сравнение с предыдущей версией...
✓ Новых шагов: 69
  + И я устанавливаю флаг "ФлагНовый"...
  + Тогда элемент формы "Новое поле" существует...

⚠ Удаленных шагов: 0
```

### Сохранение файлов

```
Сохранение файлов...
✅ ai-knowledge/steps-library.json (655 KB)
✅ data/library-full.json (680 KB)
✅ data/statistics.json
✅ ai-knowledge/README.md
✅ data/README.md
```

### Итоговая статистика

```
══════════════════════════════════════════════════════════════════
ИТОГОВАЯ СТАТИСТИКА
══════════════════════════════════════════════════════════════════

Режим: ОБНОВЛЕНИЕ ЗАВЕРШЕНО

📊 Библиотека шагов:
   Всего шагов: 1569
   Категорий: 13
   Подкатегорий: 378

🔄 Изменения:
   Новых шагов: 69
   Удаленных шагов: 0

📁 Созданные файлы:
   ai-knowledge/steps-library.json (для AI)
   ai-knowledge/README.md
   data/library-full.json
   data/statistics.json
   data/README.md

✅ База знаний успешно обновлена!

Следующие шаги:
1. Проверьте обновленные файлы
2. Закоммитьте изменения в Git
3. Загрузите новые файлы в AI для тестирования
```

---

## 📂 Создаваемые файлы

### ai-knowledge/steps-library.json
Оптимизированная база для AI (~655 KB):
```json
{
  "UI": [
    {
      "шаг": "И я открываю форму...",
      "описание": "Открытие формы",
      "тип": "UI.Формы"
    }
  ],
  "Переменные": [...],
  ...
}
```

### ai-knowledge/README.md
Инструкция с актуальной статистикой:
- Что загружать в AI
- Текущая статистика
- Топ-10 категорий
- Дата обновления

### data/library-full.json
Полная библиотека в оригинальном формате (~680 KB):
```json
[
  {
    "ИмяШага": "И я открываю форму...",
    "ОписаниеШага": "...",
    "ПолныйТипШага": "UI.Формы"
  }
]
```

### data/statistics.json
Детальная статистика:
```json
{
  "version": "1.0",
  "updated_at": "2025-11-27T15:30:00",
  "total_steps": 1569,
  "categories": {
    "UI": {
      "steps_count": 618,
      "subcategories_count": 171,
      "subcategories": ["Формы", "Таблицы", ...]
    }
  }
}
```

### data/README.md
Описание данных:
- Содержимое директории
- Инструкция по обновлению
- Текущая статистика

---

## 🔍 Режим --dry-run

Показывает что будет сделано БЕЗ применения изменений:

```bash
python update_knowledge_base.py БиблиотекаШагов.json --dry-run
```

**Полезно для:**
- Проверки перед обновлением
- Просмотра изменений
- Тестирования скрипта

**Вывод:**
```
🔍 РЕЖИМ ПРЕДПРОСМОТРА
Изменения не будут применены

✓ Загружено 1569 шагов
✓ Новых шагов: 69
⚠ Файлы НЕ будут сохранены
```

---

## ⚙️ Опции командной строки

| Опция | Описание | По умолчанию |
|-------|----------|--------------|
| `source` | Путь к БиблиотекаШагов.json | `upload/БиблиотекаШагов.json` |
| `--output-dir` | Директория для ai-knowledge | `ai-knowledge` |
| `--dry-run` | Предпросмотр без изменений | `false` |
| `--help` | Показать справку | |

---

## 🔄 Workflow обновления

### Регулярное обновление

```bash
# 1. Экспортируйте из Vanessa Automation
# (в конфигураторе или через внешнюю обработку)

# 2. Запустите предпросмотр
python update_knowledge_base.py БиблиотекаШагов.json --dry-run

# 3. Если всё ОК, применяйте
python update_knowledge_base.py БиблиотекаШагов.json

# 4. Проверьте изменения
git diff ai-knowledge/steps-library.json

# 5. Закоммитьте
git add ai-knowledge/ data/
git commit -m "Update steps library from Vanessa Automation"
git push

# 6. Обновите в AI
# Загрузите новый ai-knowledge/steps-library.json в Claude/ChatGPT
```

---

## 📈 Сравнение версий

Скрипт автоматически сравнивает с предыдущей версией:

### Что показывается

**Новые шаги:**
```
✓ Новых шагов: 69
  + И я устанавливаю флаг "ФлагНовый"
  + Тогда элемент формы "Новое поле" существует
  ...
```

**Удаленные шаги:**
```
⚠ Удаленных шагов: 5
  - И я делаю устаревшее действие
  - Тогда проверяю старый шаг
  ...
```

**Без изменений:**
```
✓ Новых шагов: 0
✓ Удаленных шагов: 0
```

---

## 🐛 Решение проблем

### Ошибка: Файл не найден

```
✗ Файл не найден: БиблиотекаШагов.json
```

**Решение:**
- Проверьте путь к файлу
- Используйте полный путь: `/full/path/to/БиблиотекаШагов.json`

### Ошибка: Неверный формат JSON

```
✗ Ошибка парсинга JSON: Unexpected token
```

**Решение:**
- Проверьте что файл экспортирован правильно
- Откройте в текстовом редакторе
- Проверьте кодировку (должна быть UTF-8)

### Предупреждение: Старая база не найдена

```
⚠ Старая база знаний не найдена (первое создание)
```

**Нормально:** При первом запуске скрипта

### Много удаленных шагов

```
⚠ Удаленных шагов: 150
```

**Проверьте:**
- Правильная ли версия БиблиотекаШагов.json?
- Не используете ли библиотеку из другого проекта?
- Сравните с документацией Vanessa Automation

---

## 🔐 Безопасность

### Резервное копирование

Перед обновлением создайте бэкап:

```bash
# Бэкап старой базы
cp ai-knowledge/steps-library.json ai-knowledge/steps-library.json.backup

# Запустите обновление
python update_knowledge_base.py БиблиотекаШагов.json

# Если что-то пошло не так:
mv ai-knowledge/steps-library.json.backup ai-knowledge/steps-library.json
```

### Git

Всегда коммитьте перед обновлением:

```bash
git add -A
git commit -m "Before library update"
python update_knowledge_base.py БиблиотекаШагов.json
```

Если нужно откатить:
```bash
git reset --hard HEAD
```

---

## 💡 Рекомендации

### Как часто обновлять?

- **При обновлении Vanessa Automation** - обязательно
- **При добавлении кастомных шагов** - обязательно
- **Регулярно** - раз в месяц для проверки

### Проверка после обновления

1. **Просмотрите изменения:**
   ```bash
   git diff ai-knowledge/steps-library.json
   ```

2. **Проверьте статистику:**
   ```bash
   cat data/statistics.json | jq .
   ```

3. **Тестируйте в AI:**
   - Загрузите новый файл в Claude/ChatGPT
   - Создайте тестовый сценарий
   - Убедитесь что новые шаги распознаются

### CI/CD интеграция

Автоматизируйте обновление в CI/CD:

```yaml
# .github/workflows/update-library.yml
name: Update Library

on:
  workflow_dispatch:
    inputs:
      library_file:
        description: 'Path to БиблиотекаШагов.json'
        required: true

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      
      - name: Setup Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.8'
      
      - name: Update Knowledge Base
        run: |
          python update_knowledge_base.py ${{ github.event.inputs.library_file }}
      
      - name: Commit changes
        run: |
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
          git add ai-knowledge/ data/
          git commit -m "Auto-update: library from Vanessa Automation"
          git push
```

---

## 📚 Дополнительная информация

### Формат БиблиотекаШагов.json

Ожидаемый формат из Vanessa Automation:

```json
[
  {
    "ИмяШага": "И я открываю форму \"ИмяФормы\"",
    "ОписаниеШага": "Открытие формы по имени",
    "ПолныйТипШага": "UI.Формы.Открытие"
  }
]
```

### Оптимизация для AI

Скрипт группирует шаги по основным категориям:

**Исходный формат** (плоский массив):
- Все 1569 шагов в одном списке
- AI должен искать по всему списку

**Оптимизированный** (по категориям):
- UI: 618 шагов
- Служебные: 453 шагов
- Прочее: 282 шагов
- ...

AI быстрее находит нужную категорию и шаг.

---

## 🎓 FAQ

**Q: Можно ли обновлять вручную?**  
A: Да, но скрипт автоматизирует процесс и создает все нужные файлы.

**Q: Что если в новой библиотеке меньше шагов?**  
A: Скрипт покажет количество удаленных шагов. Проверьте что это правильная версия библиотеки.

**Q: Нужно ли перезагружать файлы в AI?**  
A: Да, после обновления загрузите новый `steps-library.json` в AI.

**Q: Сохраняются ли guide.md и templates.md?**  
A: Да, обновляется только `steps-library.json`. Остальные файлы не трогаются.

**Q: Можно ли откатить обновление?**  
A: Да, через Git или из резервной копии.

---

## 🔗 См. также

- [Документация Vanessa Automation](https://pr-mex.github.io/vanessa-automation/dev/)
- [Экспорт библиотеки шагов](https://pr-mex.github.io/vanessa-automation/dev/export/)
- [Структура проекта](../docs/project-structure.md)

---

**Версия:** 1.0  
**Дата:** Ноябрь 2025
