# Примеры использования search-steps.py

> **Версия:** 1.0  
> **Инструмент:** Умный поиск шагов Vanessa Automation

---

## 📖 Оглавление

1. [Базовые примеры](#базовые-примеры)
2. [Фильтрация](#фильтрация)
3. [Форматы вывода](#форматы-вывода)
4. [Специальные команды](#специальные-команды)
5. [Практические сценарии для AI](#практические-сценарии-для-ai)

---

## Базовые примеры

### 1. Простой поиск
```bash
python search_steps.py --query "нажать кнопку"
```

**Результат:**
```json
{
  "query": "нажать кнопку",
  "found": 10,
  "results": [
    {
      "step": "И я нажимаю на кнопку \"Заголовок кнопки\"",
      "category": "UI",
      "relevance": 0.59,
      "subcategory": "Формы.Кнопки"
    }
  ]
}
```

### 2. Ограничение количества результатов
```bash
python search_steps.py --query "открыть форму" --top 3
```

### 3. Batch search (несколько запросов)
```bash
python search_steps.py --query "открыть" "закрыть" "заполнить" --top 5
```

**Результат:**
```json
{
  "total_queries": 3,
  "total_results": 15,
  "results": {
    "открыть": [...],
    "закрыть": [...],
    "заполнить": [...]
  }
}
```

---

## Фильтрация

### 4. Поиск только в категории UI
```bash
python search_steps.py --query "таблица добавить" --category UI --top 5
```

### 5. Точная фильтрация по подкатегории
```bash
python search_steps.py --query "кнопка" --category UI --subcategory "Формы.Кнопки"
```

**Применение:** Когда нужны только шаги для работы с кнопками форм

### 6. Поиск в категории Переменные
```bash
python search_steps.py --query "запомнить значение" --category "Переменные" --top 10
```

---

## Форматы вывода

### 7. JSON формат (по умолчанию, для AI)
```bash
python search_steps.py --query "документ провести" --format json
```

**Применение:** Оптимален для парсинга AI-агентами

### 8. Human-friendly формат
```bash
python search_steps.py --query "открыть документ" --top 3 --format human
```

**Результат:**
```
══════════════════════════════════════════════════════════════════════
ПОИСК ШАГОВ VANESSA AUTOMATION
══════════════════════════════════════════════════════════════════════

Запрос: "открыть документ"
Найдено: 3 шагов

1. [UI | 56%] Тогда открылось окно "Заголовок окна"
   └─ Окна
2. [UI | 50%] Дано Я открываю основную форму документа "ИмяДокумента"
   └─ Навигационные ссылки
3. [UI | 48%] И я жду открытия окна "Заголовок окна" в течение 20 секунд
   └─ Окна
══════════════════════════════════════════════════════════════════════
```

**Применение:** Для ручного анализа результатов в терминале

### 9. YAML-compact формат
```bash
python search_steps.py --query "поле" "кнопка" --top 2 --format yaml-compact
```

**Результат:**
```yaml
results:
  поле:
    - И в поле "Заголовок поля" я ввожу текст "ЗначениеПоля" [UI, 58%]
    - И поле "Заголовок поля" заполнено [UI, 42%]
  кнопка:
    - И я нажимаю на кнопку "Заголовок кнопки" [UI, 33%]
    - И кнопка "Заголовок кнопки" существует [UI, 43%]
```

**Применение:** Экономия токенов при передаче AI-агенту

---

## Специальные команды

### 10. Получить все шаги категории
```bash
python search_steps.py --get-category "Переменные"
```

**Результат:** Все 36 шагов категории Переменные

**Применение:** AI загружает все шаги для работы с переменными сразу

### 11. Получить шаги подкатегории
```bash
python search_steps.py --get-category "UI" --subcategory "Формы.Кнопки"
```

### 12. Статистика библиотеки
```bash
python search_steps.py --stats
```

**Результат:**
```json
{
  "total_steps": 1569,
  "total_categories": 13,
  "categories": {
    "UI": 618,
    "Служебные": 453,
    "Прочее": 282,
    ...
  }
}
```

### 13. Статистика в human формате
```bash
python search_steps.py --stats --format human
```

---

## Практические сценарии для AI

### 14. AI генерирует сценарий создания документа

**Промпт для AI:**
```
Создай сценарий для создания заказа покупателя.

Перед генерацией найди релевантные шаги:
```

**Команда:**
```bash
python tools/search-steps/search_steps.py \
  --query "открыть документ" "создать документ" "заполнить таблицу" "провести" \
  --category UI \
  --top 5
```

**Применение:** AI получает ~20 релевантных шагов и генерирует сценарий

### 15. AI исправляет ошибку валидации

**Ситуация:**
```
Валидатор: Шаг "И я нажимаю кнопку Записать" не найден
```

**Команда:**
```bash
python tools/search-steps/search_steps.py \
  --query "нажать кнопку записать" \
  --category UI \
  --top 3
```

**Результат:**
```json
{
  "results": [
    {
      "step": "И я нажимаю на кнопку \"Заголовок кнопки\"",
      "relevance": 0.85
    }
  ]
}
```

**Применение:** AI находит правильный вариант и исправляет ошибку

### 16. Комплексный поиск для сценария работы с таблицами

**Команда:**
```bash
python tools/search-steps/search_steps.py \
  --query "таблица добавить" "таблица редактировать" "таблица удалить" "таблица выбрать" \
  --category UI \
  --subcategory "Таблицы.Добавить строку" \
  --top 3 \
  --format human
```

---

## Советы по использованию

### Для AI-агентов

1. **Используйте batch search** - за один вызов получаете все нужные шаги
2. **JSON формат** - оптимален для парсинга
3. **Фильтруйте по категориям** - уменьшает количество результатов
4. **top 5-10** - баланс между релевантностью и охватом

### Для ручного использования

1. **Human формат** - читаемый вывод в терминале
2. **--stats** - обзор библиотеки
3. **--get-category** - изучение шагов категории
4. **YAML-compact** - компактный вывод для быстрого просмотра

### Производительность

- Одиночный запрос: < 50 мс
- Batch search (10 запросов): ~155 мс (с индексами), ~195 мс (без)
- Загрузка индексов: автоматическая при запуске
- Fallback: работает даже без индексов

---

## Интеграция с Kilo Code

### Пример промпта для AI

```markdown
Kilo Code, создай сценарий для создания и проведения документа.

WORKFLOW:
1. Найди релевантные шаги:
   python tools/search-steps/search_steps.py \
     --query "открыть документ" "создать" "таблица" "провести" \
     --category UI \
     --top 5

2. Используй найденные шаги для генерации

3. Валидируй через validate.py

4. Исправь по рекомендациям
```

---

**Версия:** 1.0  
**Дата:** Ноябрь 2025