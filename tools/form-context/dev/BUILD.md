# Сборка FormContextCollector.epf

> Как собрать EPF-файл обработки из исходников XML

---

## 🎯 Быстрый старт

### Сборка EPF из исходников

```bash
cd tools/form-context
build-epf.bat
```

Результат: создаётся файл `FormContextCollector.epf`

### Разборка EPF обратно в исходники

```bash
cd tools/form-context
unpack-epf.bat
```

Результат: обновляется каталог `src/` с исходниками

---

## 📋 Требования

- **1С:Предприятие 8.3** (любая версия от 8.3.10+)
- **Windows** (скрипты для командной строки Windows)
- **Права доступа** для создания временных файлов

---

## 🔧 Что делает build-epf.bat

### Шаг 1: Поиск 1cv8.exe

Скрипт автоматически ищет платформу 1С в стандартных местах:
- `C:\Program Files\1cv8\`
- `C:\Program Files (x86)\1cv8\`

### Шаг 2: Проверка исходников

Проверяет наличие:
- `src/FormContextCollector.xml` (корневой файл)
- `src/FormContextCollector/` (каталог с модулями)

### Шаг 3: Создание временной ИБ

Создаёт пустую файловую ИБ в `agent/temp_ib/`

### Шаг 4: Сборка EPF

Использует команду конфигуратора:
```
/LoadExternalDataProcessorOrReportFromFiles <исходники> <выходной файл>
```

### Шаг 5: Проверка и очистка

- Проверяет создание EPF
- Удаляет временную ИБ
- Выводит статистику

---

## 🔄 Что делает unpack-epf.bat

### Обратный процесс

1. Проверяет наличие `FormContextCollector.epf`
2. Создаёт временную ИБ
3. Разбирает EPF командой:
   ```
   /DumpExternalDataProcessorOrReportToFiles <каталог> <EPF> -Format Hierarchical
   ```
4. Сохраняет результат в `src/`

---

## 📁 Структура файлов

### До сборки (исходники)

```
tools/form-context/
├── src/                              # Исходники XML
│   ├── FormContextCollector.xml      # Корневой файл
│   └── FormContextCollector/
│       ├── Ext/
│       │   └── ObjectModule.bsl      # Модуль объекта
│       └── Forms/
│           └── Форма/
│               └── Ext/
│                   ├── Form.xml
│                   └── Form/
│                       └── Module.bsl
├── build-epf.bat                     # Скрипт сборки
└── unpack-epf.bat                    # Скрипт разборки
```

### После сборки

```
tools/form-context/
├── FormContextCollector.epf          # ← РЕЗУЛЬТАТ!
├── agent/
│   └── build.log                     # Лог сборки
└── ...
```

---

## 🐛 Решение проблем

### Ошибка: 1cv8.exe не найден

**Причина:** Платформа 1С не установлена или установлена в нестандартное место

**Решение:**
1. Установите 1С:Предприятие 8.3
2. Или отредактируйте `build-epf.bat`, найдите секцию `V8_PATHS` и добавьте ваш путь:
   ```bat
   set "V8_PATHS=%V8_PATHS%;C:\Ваш\Путь\К\1cv8.exe"
   ```

### Ошибка: Файл не создан

**Причина:** Ошибка в исходных XML-файлах

**Решение:**
1. Проверьте лог: `agent/build.log`
2. Убедитесь что XML-файлы корректны
3. Попробуйте открыть исходники в конфигураторе вручную

### Ошибка: Не удалось удалить старый EPF

**Причина:** Файл используется другим процессом

**Решение:**
1. Закройте 1С
2. Закройте конфигуратор
3. Попробуйте снова

---

## 📖 Параметры командной строки 1С

### /LoadExternalDataProcessorOrReportFromFiles

**Синтаксис:**
```
1cv8 DESIGNER /F <ИБ> 
  /LoadExternalDataProcessorOrReportFromFiles <корневой_каталог> <файл.epf>
  /DisableStartupDialogs
  /Out <лог>
```

**Описание:**
- Загружает внешнюю обработку из XML-файлов
- Автоматически определяет формат (линейный/иерархический)
- Создаёт EPF или ERF файл

**Документация:** 
См. `upload/partial-load-config/Приложение 7...pdf`, раздел 7.4.8

### /DumpExternalDataProcessorOrReportToFiles

**Синтаксис:**
```
1cv8 DESIGNER /F <ИБ>
  /DumpExternalDataProcessorOrReportToFiles <каталог> <файл.epf> -Format Hierarchical
  /DisableStartupDialogs
  /Out <лог>
```

**Описание:**
- Разбирает EPF/ERF в XML-файлы
- `-Format Hierarchical` - иерархический формат (рекомендуется)
- `-Format Plain` - линейный формат

---

## 🔗 См. также

- [README.md](README.md) - основная документация
- [QUICK_START.md](QUICK_START.md) - быстрый старт
- [Документация 1С](../../upload/partial-load-config/) - параметры командной строки

---

## 💡 Советы

### Для разработки

Рекомендуемый workflow:

1. **Разработка в конфигураторе:**
   - Откройте EPF в конфигураторе
   - Внесите изменения
   - Сохраните EPF

2. **Извлечение исходников:**
   ```bash
   unpack-epf.bat
   ```

3. **Коммит в Git:**
   ```bash
   git add src/
   git commit -m "Обновлена логика сборщика контекста"
   ```

4. **Сборка для распространения:**
   ```bash
   build-epf.bat
   ```

### Для CI/CD

Скрипты можно использовать в автоматизации:

```bat
rem Сборка без пауз
build-epf.bat
if errorlevel 1 exit /b 1

rem Копирование результата
copy FormContextCollector.epf ..\..\features\
```

---

## ✅ Контрольный список

Перед коммитом изменений:

- [ ] Исходники обновлены (`unpack-epf.bat`)
- [ ] EPF собирается без ошибок (`build-epf.bat`)
- [ ] EPF проверен в рабочей базе
- [ ] Обновлена документация (если нужно)
- [ ] Лог-файлы не добавлены в Git
- [ ] Временные файлы не добавлены в Git

---

**Версия:** 1.0  
**Дата:** Ноябрь 2025  
**Автор:** Nikolay Shirokov