# План реализации: Иерархический Markdown-формат v2.0

> **Статус:** Готов к разработке  
> **Дата создания:** 2025-11-30  
> **Приоритет:** Высокий  
> **Оценка:** 1-2 дня разработки

---

## 📋 Краткое описание

Переход от плоского JSON-формата к иерархическому Markdown с английскими тегами для оптимального восприятия AI.

### Что меняется

**Было (JSON + плоский Markdown):**
```json
{
  "elements": [
    {"name": "РольИсточник", "type": "field", "kind": "input"},
    ...
  ]
}
```
```markdown
## 📝 Поля формы
- `РольИсточник` [input]
```

**Станет (только иерархический Markdown):**
```markdown
# Form: АнализПолномочийРолей
- [Group] "Основная группа" (ГруппаОсновная)
  - [Input] "Роль-источник" (РольИсточник) *Required
  - [Table] "Роли-приемники" (РолиПриемники)
    - [CmdBar] "Команды" (РолиПриемникиКоманднаяПанель)
      - [Button] "Добавить" (РолиПриемникиДобавить)
    - [Col] "Роль" (РолиПриемникиРоль)
```

### Преимущества
- ✅ Экономия токенов ~60% (40 строк вместо 900 JSON)
- ✅ Сохранена полная иерархия структуры
- ✅ Английские теги понятнее для AI
- ✅ Контекст вложенности ("кнопка внутри таблицы")
- ✅ Состояния элементов (*Required, *ReadOnly, {Hidden})

---

## 🎯 Техническое задание

Полная спецификация: [`ФорматСтруктурыФорм.md`](./ФорматСтруктурыФорм.md)

### Ключевые требования

1. **Формат строки элемента:**
   ```
   - [Тег] "Заголовок" (ИмяЭлемента) {Опции}
   ```

2. **Отступы:** 2 пробела на каждый уровень вложенности

3. **Логика видимости (важно!):**
   - Режим "Только видимые" (по умолчанию): скрытые элементы пропускаются полностью
   - Режим "Выгружать скрытые": скрытые элементы помечаются `{Hidden}`
   - **Оптимизация:** Если скрытый элемент = контейнер (Group, Tab, Table), то его дочерние элементы НЕ обходятся

4. **Определение заголовка (каскад):**
   - Сначала `Элемент.Заголовок`
   - Если пусто → `Элемент.Подсказка`
   - Если пусто → `Элемент.РасширеннаяПодсказка.Заголовок`
   - Если пусто → `Элемент.Имя`

5. **Состояния элементов:**
   - `*Required` если `АвтоОтметкаНезаполненного = Истина`
   - `*ReadOnly` если `ТолькоПросмотр = Истина` ИЛИ `Доступность = Ложь`
   - `{Hidden}` если `Видимость = Ложь` (только в режиме "выгружать скрытые")

---

## 🗺️ Маппинг типов элементов

### Типы элементов → Теги

| Тип 1С | Проверка | Тег AI |
|--------|----------|---------|
| ПолеФормы | `ТипЗнч() = Тип("ПолеФормы")` | `[Input]` / `[Checkbox]` / `[Radio]` / `[Label]` (зависит от Вид) |
| КнопкаФормы | `ТипЗнч() = Тип("КнопкаФормы")` | `[Button]` |
| ТаблицаФормы | `ТипЗнч() = Тип("ТаблицаФормы")` | `[Table]` |
| ГруппаФормы | `ТипЗнч() = Тип("ГруппаФормы")` | `[Group]` / `[Tab]` / `[Menu]` / `[CmdBar]` / `[Pages]` (зависит от Вид) |
| ДекорацияФормы | `ТипЗнч() = Тип("ДекорацияФормы")` | `[Label]` / `[Link]` / `[Image]` |
| Служебные | `Тип("ДополнениеЭлементаФормы")` | `skip` (пропускаем) |

### Виды полей → Теги

| ВидПоляФормы | Тег |
|--------------|-----|
| `ВидПоляФормы.ПолеВвода` | `[Input]` |
| `ВидПоляФормы.ПолеФлажка` | `[Checkbox]` |
| `ВидПоляФормы.ПолеПереключателя` | `[Radio]` |
| `ВидПоляФормы.ПолеНадписи` | `[Label]` |
| `ВидПоляФормы.ПолеКартинки` | `[Image]` |
| `ВидПоляФормы.ПолеТекстовогоДокумента` | `[TextEditor]` |
| `ВидПоляФормы.ПолеHTMLДокумента` | `[HTML]` |
| `ВидПоляФормы.ПолеТабличногоДокумента` | `[Spreadsheet]` |
| `ВидПоляФормы.ПолеКалендаря` | `[Calendar]` |

### Виды групп → Теги

| ВидГруппыФормы | Тег |
|----------------|-----|
| `ВидГруппыФормы.КоманднаяПанель` | `[CmdBar]` |
| `ВидГруппыФормы.ГруппаКнопок` | `[Menu]` |
| `ВидГруппыФормы.Страницы` | `[Pages]` |
| `ВидГруппыФормы.Страница` | `[Tab]` |
| Остальные | `[Group]` |

### Поля внутри таблиц

Все поля (`ПолеФормы`) внутри `ТаблицаФормы` должны иметь тег `[Col]` вместо `[Input]`/`[Checkbox]` и т.д.

---

## 📐 Архитектура решения

### Структура функций

```
СгенерироватьMarkdownНовыйФормат(Форма)
  ↓
  1. Формируем заголовок: "# Form: {Заголовок формы}"
  ↓
  2. Обходим Форма.ПодчиненныеЭлементы
     └─ Для каждого элемента:
        СформироватьСтрокиЭлементаРекурсивно(Элемент, Глубина=0)
  ↓
  3. Добавляем ФормаКоманднаяПанель (если есть)
```

### Основная рекурсивная функция

```bsl
Функция СформироватьСтрокиЭлементаРекурсивно(Элемент, Глубина, ВыгружатьСкрытые)
    
    // 1. ПРОВЕРКА ВИДИМОСТИ
    Видимость = Элемент.Видимость; // может выбросить исключение
    
    Если Не Видимость Тогда
        Если Не ВыгружатьСкрытые Тогда
            Возврат ""; // Пропускаем полностью
        КонецЕсли;
        
        СкрытФлаг = Истина;
        
        // Если это контейнер (Group, Table, Tab) - не обходим детей
        Если ЭтоКонтейнер(Элемент) Тогда
            ПропускатьДетей = Истина;
        КонецЕсли;
    КонецЕсли;
    
    // 2. ФОРМИРОВАНИЕ СТРОКИ
    Отступ = СтрПовтор("  ", Глубина);
    Тег = ОпределитьТегЭлемента(Элемент);
    
    Если Тег = "skip" Тогда
        Возврат ""; // Служебный элемент
    КонецЕсли;
    
    Заголовок = ОпределитьЗаголовокДляAI(Элемент);
    Опции = СформироватьОпции(Элемент, СкрытФлаг);
    
    // Формат: - [Tag] "Title" (Name) {Options}
    Результат = СтрШаблон("%1- [%2] ""%3"" (%4)%5",
        Отступ, Тег, Заголовок, Элемент.Имя,
        ?(ПустаяСтрока(Опции), "", " " + Опции)
    ) + Символы.ПС;
    
    // 3. РЕКУРСИЯ
    Если Не ПропускатьДетей Тогда
        Для Каждого ДочернийЭлемент Из Элемент.ПодчиненныеЭлементы Цикл
            Результат = Результат + СформироватьСтрокиЭлементаРекурсивно(
                ДочернийЭлемент, Глубина + 1, ВыгружатьСкрытые
            );
        КонецЦикла;
    КонецЕсли;
    
    Возврат Результат;
КонецФункции
```

---

## 🔧 Список изменений в коде

### Файл: `Module.bsl` (строки 844-950)

#### 1. Создать новую функцию (после строки 843)

```bsl
// Генерирует Markdown в новом иерархическом формате v2.0
//
// Параметры:
//   Форма - УправляемаяФорма - Форма для генерации
//
// Возвращаемое значение:
//   Строка - Markdown текст в иерархическом формате
//
&НаКлиенте
Функция СгенерироватьMarkdownНовыйФормат(Форма)
    Результат = "";
    
    // Заголовок формы
    Результат = Результат + "# Form: " + ПолучитьЗаголовокФормы(Форма) + Символы.ПС;
    
    // Рекурсивно обходим элементы верхнего уровня
    КоллекцияКорневых = Форма.ПодчиненныеЭлементы;
    
    Для Каждого Элемент Из КоллекцияКорневых Цикл
        СтрокиЭлемента = СформироватьСтрокиЭлементаРекурсивно(
            Элемент, 
            0, 
            Объект.ВключатьНевидимыеЭлементы
        );
        Результат = Результат + СтрокиЭлемента;
    КонецЦикла;
    
    // Добавляем командную панель формы
    Попытка
        КП = Форма.Элементы.ФормаКоманднаяПанель;
        СтрокиКП = СформироватьСтрокиЭлементаРекурсивно(
            КП, 0, Объект.ВключатьНевидимыеЭлементы
        );
        Результат = Результат + СтрокиКП;
    Исключение
    КонецПопытки;
    
    Возврат Результат;
КонецФункции
```

#### 2. Создать вспомогательные функции

См. полный код в [`TECHNICAL_DESIGN.md`](#) (будет создан).

#### 3. Обновить вызов в [`СобратьКонтекст()`](Module.bsl:108)

Строка 162: заменить
```bsl
ТекстMarkdown = СгенерироватьMarkdown(ДанныеФормы);
```
на:
```bsl
ТекстMarkdown = СгенерироватьMarkdownНовыйФормат(СсылкаНаФорму);
```

#### 4. Сделать JSON опциональным

Добавить в форму:
```
☐ Генерировать JSON (для программной обработки)
```

По умолчанию `Ложь`. Если включено - генерируем оба файла.

---

## ✅ Чеклист реализации

### Этап 1: Маппинг типов (30 мин)

- [ ] Функция `ОпределитьТегЭлемента(Элемент)` → возврат строки-тега
- [ ] Функция `ОпределитьТегПоля(ПолеФормы)` → маппинг ВидПоляФормы
- [ ] Функция `ОпределитьТегГруппы(ГруппаФормы)` → маппинг ВидГруппыФормы
- [ ] Функция `ОпределитьТегДекорации(ДекорацияФормы)` → Label/Link/Image
- [ ] Функция `ЭтоКонтейнер(Элемент)` → проверка на Group/Table/Tab

### Этап 2: Определение текста и состояний (30 мин)

- [ ] Функция `ОпределитьЗаголовокДляAI(Элемент)` → каскад Заголовок → Подсказка → ВП → Имя
- [ ] Функция `СформироватьОпции(Элемент, СкрытФлаг)` → *Required, *ReadOnly, {Hidden}
- [ ] Функция `ПроверитьТребуетсяОтметка(Элемент)` → АвтоОтметкаНезаполненного
- [ ] Функция `ПроверитьТолькоЧтение(Элемент)` → ТолькоПросмотр ИЛИ Не Доступность

### Этап 3: Рекурсивная генерация (1 час)

- [ ] Функция `СформироватьСтрокиЭлементаРекурсивно(Элемент, Глубина, ВыгружатьСкрытые)`
  - [ ] Проверка видимости с правильной логикой (п. 3.1 ТЗ)
  - [ ] Формирование строки с отступами
  - [ ] Рекурсия с условием (не обходим детей скрытых контейнеров)
- [ ] Функция `СгенерироватьMarkdownНовыйФормат(Форма)`
  - [ ] Формирование заголовка
  - [ ] Обход корневых элементов
  - [ ] Добавление командной панели формы

### Этап 4: Интеграция (30 мин)

- [ ] Обновить вызов в `СобратьКонтекст()` (строка 162)
- [ ] Добавить чекбокс "Генерировать JSON" в форму
- [ ] Условно генерировать JSON (только если включено)
- [ ] Обновить функцию `ОбновитьИндексФорм()` (убрать `json_path` если JSON не генерируется)

### Этап 5: Специальные случаи (30 мин)

- [ ] Обработка колонок таблиц (тег `[Col]` вместо `[Input]`)
- [ ] Обработка командных панелей таблиц
- [ ] Обработка вложенных меню (Menu внутри Menu)

### Этап 6: Тестирование (1 час)

- [ ] Тест на простой форме (только поля)
- [ ] Тест на форме с таблицей
- [ ] Тест на форме со скрытыми элементами
- [ ] Тест на форме с вложенными группами/вкладками
- [ ] Проверка размера выходных файлов (должно быть ~60% экономии)

---

## 📊 Примеры вывода

### Простая форма

**Форма:** Обработка.АнализПолномочийРолей

**Вывод:**
```markdown
# Form: АнализПолномочийРолей
- [Group] "Основная группа" (ГруппаОсновная)
  - [Input] "Роль-источник" (РольИсточник) *Required
  - [Input] "Маска" (МаскаИмениРолиПриемника)
  - [Checkbox] "Показывать профили" (ПоказыватьПрофили)
  - [Table] "Роли-приемники" (РолиПриемники)
    - [CmdBar] "Команды" (РолиПриемникиКоманднаяПанель)
      - [Button] "Добавить" (РолиПриемникиДобавить)
    - [Col] "Отметка" (РолиПриемникиОтметка)
    - [Col] "Роль" (РолиПриемникиРоль) *Required
    - [Col] "Профили" (РолиПриемникиПрофили)
  - [TextEditor] "Результат" (РезультатАнализа) *ReadOnly
- [CmdBar] "Командная панель" (ФормаКоманднаяПанель)
  - [Button] "Анализировать" (ФормаАнализировать)
  - [Button] "Выгрузить отчет" (ФормаВыгрузитьОтчет)
  - [Button] "Сохранить соответствие" (ФормаСохранитьСоответствие)
  - [Button] "Загрузить соответствие" (ФормаЗагрузитьСоответствие)
```

**Размер:** ~20 строк, ~800 символов (вместо ~900 строк JSON)

### Форма со скрытыми элементами

**Режим:** ВыгружатьСкрытые = Истина

**Вывод:**
```markdown
# Form: ДокументЗаказПоставщику
- [CmdBar] "Командная панель" (ФормаКоманднаяПанель)
  - [Button] "Провести и закрыть" (ФормаПровестиИЗакрыть)
- [Group] "Шапка" (ГруппаШапка)
  - [Input] "Организация" (Организация) *Required
  - [Input] "Контрагент" (Контрагент) *Required
  - [Group] "Служебная информация" (ГруппаСлужебная) {Hidden}
    // НЕТ дочерних элементов - контейнер скрыт
  - [Input] "Валюта" (Валюта) *ReadOnly
- [Pages] "Страницы" (ГруппаСтраницы)
  - [Tab] "Товары" (СтраницаТовары)
    - [Table] "Товары" (Товары)
      - [Col] "Номенклатура" (ТоварыНоменклатура) *Required
      - [Col] "Количество" (ТоварыКоличество)
  - [Tab] "Доставка" (СтраницаДоставка) {Hidden}
```

---

## 🚀 Следующие шаги

1. **Переключиться в режим Code:** `@switch_mode code`
2. **Начать с Этапа 1:** Создание функций маппинга типов
3. **После реализации:** Протестировать на реальных формах
4. **После тестов:** Обновить документацию и примеры

---

## 📚 Связанные документы

- [`ФорматСтруктурыФорм.md`](./ФорматСтруктурыФорм.md) - полное ТЗ
- [`SIMPLIFIED_ARCHITECTURE.md`](./SIMPLIFIED_ARCHITECTURE.md) - общая архитектура (требует обновления)
- [`Module.bsl`](./src/FormContextCollector/Forms/Форма/Ext/Form/Module.bsl) - код для изменения

---

**Автор:** AI Architect  
**Проверен:** ✅ Готов к разработке  
**Следующий режим:** Code