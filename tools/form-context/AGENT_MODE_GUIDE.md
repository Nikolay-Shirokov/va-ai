# Руководство по агентскому режиму

> Автоматический сбор контекста форм без ручного вмешательства

---

## 🚀 Быстрый старт

### 1. Подготовка списка форм

Создайте файл со списком форм (одна форма на строку):

```bash
cat > forms.txt << EOF
Документ.ЗаказПокупателя.Форма.ФормаДокумента
Справочник.Контрагенты.Форма.ФормаЭлемента
Документ.ЗаказПокупателя.Форма.ФормаСписка
EOF
```

### 2. Запуск сбора

```bash
python tools/form-context/form_context_cli.py agent \
    --infobase "File=C:/Bases/Test/" \
    --forms-file forms.txt \
    --wait
```

### 3. Проверка результатов

```bash
ls -la context/forms/
cat context/forms/index.json
```

---

## 📖 Использование

### Основные команды

#### Одна форма

```bash
python form_context_cli.py agent \
    --infobase "File=C:/Bases/Test/" \
    --forms "Документ.ЗаказПокупателя.Форма.ФормаДокумента"
```

#### Несколько форм

```bash
python form_context_cli.py agent \
    --infobase "File=C:/Bases/Test/" \
    --forms "Документ.ЗаказПокупателя" "Справочник.Контрагенты"
```

#### Из файла с ожиданием

```bash
python form_context_cli.py agent \
    --infobase "File=C:/Bases/Test/" \
    --forms-file forms.txt \
    --wait
```

#### Без закрытия 1С

```bash
python form_context_cli.py agent \
    --infobase "File=C:/Bases/Test/" \
    --forms "Документ.ЗаказПокупателя" \
    --no-close
```

### Параметры

| Параметр | Описание | Обязательный |
|----------|----------|--------------|
| `--infobase` | Строка подключения к базе | ✅ Да |
| `--forms` | Список форм (разделенные пробелом) | ⚠️ Да (или --forms-file) |
| `--forms-file` | Файл со списком форм | ⚠️ Да (или --forms) |
| `--wait` | Ждать завершения обработки | ❌ Нет |
| `--timeout` | Таймаут ожидания (сек) | ❌ Нет (по умолчанию 300) |
| `--include-invisible` | Включать невидимые элементы | ❌ Нет |
| `--no-markdown` | Не генерировать Markdown | ❌ Нет |
| `--no-close` | Не закрывать 1С после завершения | ❌ Нет |

---

## 📝 Форматы форм

### 1. Полный путь к форме

```
Документ.ЗаказПокупателя.Форма.ФормаДокумента
```

### 2. Объект метаданных (откроется основная форма)

```
Справочник.Контрагенты
Документ.ЗаказПокупателя
```

### 3. Объект с указанием формы (в task.json)

```json
{
  "type": "metadata_object",
  "value": "Справочник.Контрагенты",
  "form_name": "ФормаЭлемента"
}
```

---

## 🔄 Жизненный цикл task.json

1. **`task.json`** - создается Python CLI
2. **`task.json.processing`** - во время обработки
3. **`task.json.completed`** - успешное завершение
4. **`task.json.error`** - ошибка при обработке

```bash
# Проверка статуса
ls tools/form-context/agent/task.json*
```

---

## 📊 Пример вывода

```bash
$ python form_context_cli.py agent --infobase "File=C:/Bases/Test/" --forms-file forms.txt --wait

============================================================
Form Context Collector - Agent Mode
============================================================

📋 Форм для обработки: 3
   1. Документ.ЗаказПокупателя.Форма.ФормаДокумента
   2. Справочник.Контрагенты.Форма.ФормаЭлемента
   3. Документ.ЗаказПокупателя.Форма.ФормаСписка

⚙️  Настройки:
   Включать невидимые: False
   Генерировать Markdown: True
   Закрывать 1С: True

✓ Создан файл задания: tools/form-context/agent/task.json

🚀 Запуск 1С...
  База: File=C:/Bases/Test/
  Обработка: tools/form-context/FormContextCollector.epf

⏳ Ожидание завершения (таймаут: 300с)...
   Ожидание начала обработки.....
   ✓ Обработка запущена
   Ожидание завершения.........................

============================================================
✅ Обработка завершена успешно
   Результаты сохранены в context/forms/
```

---

## 🐛 Отладка

### Логи

Логи обработки записываются в:
```
tools/form-context/debug.log
```

### Проверка статуса

```bash
# Статус задания
cat tools/form-context/agent/task.json.completed

# Проверка результатов
ls -la context/forms/
cat context/forms/index.json
```

### Ручной запуск

Если нужно запустить без CLI:

1. Создайте вручную `task.json`:
```bash
cp tools/form-context/agent/task.json.example tools/form-context/agent/task.json
```

2. Отредактируйте список форм

3. Откройте `FormContextCollector.epf` в 1С

---

## ⚠️ Возможные проблемы

### 1С не найден в PATH

```
❌ ОШИБКА: 1cv8 не найден в PATH
```

**Решение:** Добавьте путь к 1С в PATH или используйте полный путь:
```bash
# Windows
set PATH=%PATH%;C:\Program Files\1cv8\
```

### Форма не открывается

```
✗ Форма не открылась, пропускаем
```

**Причины:**
- Неправильный путь к форме
- Форма не существует в конфигурации
- Недостаточно прав

**Решение:** Проверьте путь в `debug.log`

### Таймаут ожидания

```
⚠️  Превышен таймаут ожидания
```

**Решение:** Увеличьте таймаут:
```bash
--timeout 600  # 10 минут
```

---

## 💡 Советы

### 1. Пакетная обработка

Обработка больших списков форм:

```bash
# Разбейте на части по 10 форм
split -l 10 all_forms.txt forms_batch_

# Обработайте каждую часть
for batch in forms_batch_*; do
    python form_context_cli.py agent \
        --infobase "File=C:/Bases/Test/" \
        --forms-file $batch \
        --wait
    sleep 5
done
```

### 2. Автоматизация

Создайте скрипт для регулярного обновления:

```bash
#!/bin/bash
# update_context.sh

python tools/form-context/form_context_cli.py agent \
    --infobase "File=C:/Bases/Production/" \
    --forms-file tools/form-context/important_forms.txt \
    --wait

git add context/forms/
git commit -m "chore: update form context"
```

### 3. CI/CD Integration

```yaml
# .github/workflows/update-context.yml
name: Update Form Context

on:
  schedule:
    - cron: '0 2 * * 1'  # Каждый понедельник в 2:00

jobs:
  update:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v2
      - name: Update context
        run: |
          python tools/form-context/form_context_cli.py agent \
            --infobase "${{ secrets.INFOBASE }}" \
            --forms-file tools/form-context/important_forms.txt \
            --wait
      - name: Commit results
        run: |
          git config user.name "Bot"
          git config user.email "bot@example.com"
          git add context/forms/
          git commit -m "chore: automated context update" || true
          git push
```

---

## 🔗 См. также

- [README.md](README.md) - Основная документация
- [QUICK_START.md](QUICK_START.md) - Быстрый старт (интерактивный режим)
- [SIMPLIFIED_ARCHITECTURE.md](SIMPLIFIED_ARCHITECTURE.md) - Архитектура
- [task.json.example](agent/task.json.example) - Пример файла задания

---

**Версия:** 1.0  
**Дата:** Ноябрь 2025