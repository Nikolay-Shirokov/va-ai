# Form Context Collector

> Автоматический сбор контекста форм 1С для генерации сценариев тестирования с помощью AI

[![Status](https://img.shields.io/badge/status-planned-yellow.svg)](https://github.com/Nikolay-Shirokov/va-ai)
[![Python](https://img.shields.io/badge/python-3.6+-blue.svg)](https://www.python.org/downloads/)
[![1C](https://img.shields.io/badge/1C-8.3.10+-green.svg)](https://v8.1c.ru/)

---

## 🎯 Что это?

**Form Context Collector** - инструмент для автоматического сбора минимального, но достаточного описания форм 1С, оптимизированного для AI-ассистентов (Claude, ChatGPT, и др.).

### Проблема

При написании сценариев тестирования AI нужен контекст о форме:
- Какие элементы есть?
- Как они называются?
- Какая структура?

Сейчас это собирается вручную или через встроенный Исследователь форм, который не оптимизирован для AI.

### Решение

Автоматический сбор контекста с:
- ✅ Минимальным набором данных (экономия токенов)
- ✅ AI-оптимизированной структурой
- ✅ Переиспользованием (кэширование)
- ✅ Расширяемостью (опциональные детали)

---

## 🚀 Быстрый старт

### Установка

```bash
# 1. Убедитесь что у вас установлен Python 3.6+
python --version

# 2. Установите обработку в Vanessa Automation
# (копируем FormContextCollector.epf в каталог features/)

# 3. Готово! Дополнительных зависимостей не требуется
```

### Базовое использование

```bash
# 1. Откройте форму в клиенте тестирования вручную

# 2. Запустите сбор контекста
python tools/form-context/form_context_cli.py \
    --form "Документ.ЗаказПокупателя" \
    --output context/forms/

# 3. Результат: созданы файлы
# context/forms/Документ.ЗаказПокупателя.json
# context/forms/Документ.ЗаказПокупателя.md

# 4. Используйте в промпте к AI
```

---

## 📖 Использование

### Команда

```bash
python tools/form-context/form_context_cli.py [OPTIONS]
```

### Опции

| Опция | Описание | По умолчанию |
|-------|----------|--------------|
| `--form` | Имя формы или навигационная ссылка | *обязательно* |
| `--output` | Директория для сохранения | `context/forms/` |
| `--detail-level` | Уровень детализации: `minimal`, `medium`, `full` | `minimal` |
| `--format` | Форматы вывода: `json`, `markdown`, `both` | `both` |
| `--screenshot` | Создать скриншот формы | `false` |

### Примеры

**Минимальный контекст (рекомендуется):**
```bash
python tools/form-context/form_context_cli.py \
    --form "Справочник.Контрагенты"
```

**Полный контекст со скриншотом:**
```bash
python tools/form-context/form_context_cli.py \
    --form "Документ.РеализацияТоваровУслуг" \
    --detail-level full \
    --screenshot
```

**Только JSON (для API):**
```bash
python tools/form-context/form_context_cli.py \
    --form "Отчет.ОстаткиТоваров" \
    --format json \
    --output api/cache/
```

---

## 📁 Форматы вывода

### JSON (структурированные данные)

**Файл:** `Документ.ЗаказПокупателя.json`

```json
{
  "version": "1.0",
  "collected_at": "2025-11-29T10:00:00Z",
  "form": {
    "name": "Документ.ЗаказПокупателя.Форма.ФормаДокумента",
    "title": "Заказ покупателя",
    "type": "DocumentForm"
  },
  "elements": [
    {
      "name": "Организация",
      "title": "Организация",
      "type": "TestableFormField",
      "kind": "InputField",
      "parent": null
    }
  ],
  "statistics": {
    "total_elements": 25,
    "fields": 10,
    "tables": 1,
    "buttons": 8
  }
}
```

### Markdown (AI-оптимизированный)

**Файл:** `Документ.ЗаказПокупателя.md`

```markdown
# Форма: Заказ покупателя

**Тип:** Документ.ЗаказПокупателя  
**Собрано:** 2025-11-29 10:00

---

## 📝 Основные реквизиты

- `Организация` ("Организация")
- `Дата` ("от")
- `Номер` ("№")
- `Контрагент` ("Контрагент")

## 📊 Табличные части

### Товары (`Товары`)
- `ТоварыНоменклатура` ("Номенклатура")
- `ТоварыКоличество` ("Количество")
- `ТоварыЦена` ("Цена")

## 🔘 Команды

- `ФормаЗаписать` ("Записать")
- `ФормаПровести` ("Провести")
- `ТоварыДобавить` ("Добавить")

---

**Всего элементов:** 25
```

### Компактный формат (для промптов)

```markdown
Форма: Заказ покупателя (Документ.ЗаказПокупателя)
Поля: `Организация`, `Дата`, `Номер`, `Контрагент`
Таблица `Товары`: `ТоварыНоменклатура`, `ТоварыКоличество`, `ТоварыЦена`
Кнопки: `ФормаЗаписать`, `ФормаПровести`, `ТоварыДобавить`
```

---

## 🔄 Workflow

### Сценарий 1: Разовый сбор контекста

```
1. Открыть форму в TestClient
         ↓
2. Запустить form_context_cli.py
         ↓
3. Получить файлы .json и .md
         ↓
4. Использовать .md в промпте к AI
```

### Сценарий 2: Интеграция с AI

```python
# Пример интеграции в Python-скрипт
from tools.form_context import FormContextProvider

# Получить контекст
context = FormContextProvider.get_context("Документ.ЗаказПокупателя")

# Использовать в промпте
prompt = f"""
Создай сценарий для формы:

{context.to_compact_markdown()}

Требования:
- Заполнить все поля
- Провести документ
"""
```

### Сценарий 3: Batch-режим

```bash
# Собрать контекст для нескольких форм
./collect_forms.sh forms_list.txt
```

---

## 📂 Структура данных

### Директория context/

```
context/
├── forms/                              # Описания форм
│   ├── Справочник.Контрагенты.json
│   ├── Справочник.Контрагенты.md
│   ├── Документ.ЗаказПокупателя.json
│   ├── Документ.ЗаказПокупателя.md
│   └── ...
├── index.json                          # Реестр всех форм
└── README.md
```

### index.json

```json
{
  "version": "1.0",
  "updated_at": "2025-11-29T10:00:00Z",
  "forms": {
    "Документ.ЗаказПокупателя": {
      "title": "Заказ покупателя",
      "json_path": "forms/Документ.ЗаказПокупателя.json",
      "markdown_path": "forms/Документ.ЗаказПокупателя.md",
      "collected_at": "2025-11-29T09:30:00Z",
      "element_count": 25
    }
  }
}
```

---

## ⚙️ Уровни детализации

### Minimal (по умолчанию)

**Что собирается:**
- Имя элемента
- Заголовок
- Тип (поле, кнопка, таблица)
- Вид (ввод, флажок, переключатель)
- Родитель (для иерархии)

**Размер:** ~5-10 KB на форму  
**Рекомендуется:** для большинства случаев

### Medium

**Дополнительно:**
- Текущая видимость
- Текущая доступность
- Путь к данным
- Имя команды (для кнопок)

**Размер:** ~10-20 KB на форму  
**Рекомендуется:** для сложных форм с условной логикой

### Full

**Дополнительно:**
- Все свойства элементов
- Подсказки
- Обработчики событий
- Состояние элементов

**Размер:** ~20-50 KB на форму  
**Рекомендуется:** для отладки и анализа

---

## 🎯 Примеры использования с AI

### Пример 1: Создание сценария

**Промпт:**
```
Создай сценарий создания документа "Заказ покупателя".

Контекст формы:
[вставить содержимое Документ.ЗаказПокупателя.md]

Требования:
- Заполнить все обязательные поля
- Добавить 2 строки в табличную часть
- Провести документ
- Проверить что документ проведен
```

### Пример 2: Исправление ошибок

**Промпт:**
```
Исправь ошибки в этом сценарии, используя правильные имена элементов:

[сценарий с ошибками]

Контекст формы:
[вставить Документ.ЗаказПокупателя.md]
```

### Пример 3: Оптимизация сценария

**Промпт:**
```
Оптимизируй этот сценарий, используя имена полей вместо заголовков где возможно:

[исходный сценарий]

Доступные имена элементов:
[вставить компактный формат из .md]
```

---

## 🛠️ Разработка

### Структура проекта

```
tools/form-context/
├── README.md                      # Этот файл
├── form_context_cli.py            # CLI интерфейс
├── form_context_formatter.py      # Форматирование данных
├── form_context_provider.py       # API для Python
├── 1c/
│   └── FormContextCollector.epf   # Обработка 1С
└── tests/
    ├── test_formatter.py
    └── test_provider.py
```

### Запуск тестов

```bash
# Тесты форматирования
python -m pytest tools/form-context/tests/test_formatter.py

# Тесты API
python -m pytest tools/form-context/tests/test_provider.py

# Все тесты
python -m pytest tools/form-context/tests/
```

---

## 🐛 Решение проблем

### Ошибка: Форма не найдена

```
Error: Form 'Документ.ЗаказПокупателя' not found in TestClient
```

**Решение:**
1. Убедитесь что форма открыта в клиенте тестирования
2. Проверьте правильность имени формы
3. Проверьте подключение к TestClient

### Ошибка: Не удалось собрать данные

```
Error: Failed to collect form data
```

**Решение:**
1. Проверьте что Vanessa Automation запущен
2. Проверьте что TestClient активен
3. Попробуйте перезапустить TestClient

### Предупреждение: Неполные данные

```
Warning: Some elements could not be scanned
```

**Причина:** Некоторые элементы не доступны или скрыты

**Решение:**
- Это нормально для условно видимых элементов
- Используйте `--detail-level full` для максимальной полноты

---

## 📊 Статистика

### Производительность

- Сбор контекста простой формы: < 1 секунда
- Сбор контекста сложной формы: 2-3 секунды
- Форматирование: < 0.5 секунды

### Размеры файлов

| Уровень | JSON | Markdown |
|---------|------|----------|
| minimal | 5-10 KB | 2-5 KB |
| medium | 10-20 KB | 5-10 KB |
| full | 20-50 KB | 10-20 KB |

---

## 🔗 См. также

- [Архитектурный документ](../../docs/architecture/form-context-collector.md) - детальное описание
- [Validator](../validator/README.md) - валидатор сценариев
- [Search Steps](../search-steps/README.md) - поиск шагов
- [Vanessa Automation](https://github.com/Pr-mex/vanessa-automation) - основной проект

---

## 📝 Roadmap

### v0.1 (MVP) - Планируется
- [x] Архитектура
- [ ] Обработка 1С (базовая)
- [ ] Python formatter
- [ ] CLI интерфейс
- [ ] Тесты
- [ ] Документация

### v0.2 - Будущее
- [ ] Уровни детализации
- [ ] Иерархическая структура
- [ ] Batch-режим
- [ ] Кэширование

### v1.0 - Долгосрочно
- [ ] Автоматический запуск VA
- [ ] Поддержка скриншотов
- [ ] API для интеграций
- [ ] Web UI

---

## 🤝 Вклад

Приветствуются:
- 🐛 Отчеты об ошибках
- 💡 Предложения улучшений
- 📖 Улучшения документации
- 🔧 Pull requests

---

## 📄 Лицензия

MIT License - свободное использование

---

**Версия:** 0.1.0 (в разработке)  
**Дата:** Ноябрь 2025  
**Автор:** Nikolay Shirokov