# План реализации Этапа 1: Базовый функционал сбора контекста форм

## Цель этапа

Реализовать минимальный работающий функционал обработки, который сможет:
- Подключиться к TestClient API Vanessa Automation
- Получить активную форму из клиента тестирования
- Собрать базовую информацию о форме и её элементах
- Сформировать JSON с результатами
- Сохранить результат в файл

---

## Задачи этапа

### 1. Настройка интеграции с Vanessa Automation ⚙️

**Подзадачи:**

#### 1.1. Изучить API подключения к TestClient
- **Цель**: Понять, как получить доступ к TestClient из внешней обработки
- **Действия**:
  - Проанализировать код [`upload/vanessa-automation-single/vanessa-automation-single/Forms/ИсследовательФормы/Ext/Form/Module.bsl`](../../upload/vanessa-automation-single/vanessa-automation-single/Forms/ИсследовательФормы/Ext/Form/Module.bsl)
  - Найти методы инициализации TestClient
  - Определить необходимые параметры подключения
- **Критерий готовности**: Документирована последовательность вызовов для получения TestClient

#### 1.2. Реализовать функцию `ИнициализироватьТестируемуюФорму()`
- **Цель**: Получить ссылку на активную форму в клиенте тестирования
- **Файл**: [`tools/form-context/src/FormContextCollector/Forms/Форма/Ext/Form/Module.bsl`](src/FormContextCollector/Forms/Форма/Ext/Form/Module.bsl)
- **Требования**:
  - Получение доступа к Vanessa Automation через контекст
  - Получение `ТестируемоеОкноКлиентскогоПриложения`
  - Получение активного окна через `АктивноеОкно()`
  - Проверка наличия активной формы
  - Обработка ошибок с понятными сообщениями
- **Критерий готовности**: 
  - Функция возвращает объект `ТестируемаяФорма`
  - При отсутствии активной формы выдается понятное сообщение

#### 1.3. Добавить переменные модуля формы
- **Цель**: Создать структуру для хранения данных в процессе работы
- **Переменные**:
  ```bsl
  Перем ТестируемоеОкно;        // ТестируемоеОкноКлиентскогоПриложения
  Перем ТестируемаяФорма;       // ТестируемаяФорма
  Перем ТекущаяГлубина;         // Число - для отслеживания вложенности
  Перем МенеджерVA;             // Ссылка на менеджер Vanessa Automation
  ```
- **Критерий готовности**: Переменные объявлены в начале модуля

---

### 2. Реализация обработчиков формы 📝

#### 2.1. Обработчик `ПриСозданииНаСервере()`
- **Цель**: Инициализация формы при открытии
- **Действия**:
  ```bsl
  &НаСервере
  Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
      // Инициализация значений по умолчанию для реквизитов
      Если ПустаяСтрока(Объект.ПутьКФайлуВыгрузки) Тогда
          Объект.ПутьКФайлуВыгрузки = ПолучитьИмяВременногоФайла("json");
      КонецЕсли;
      
      // Установка значений по умолчанию для настроек
      Объект.ВключатьТабличныеЧасти = Истина;
      Объект.ВключатьКоманды = Истина;
      Объект.МаксимальнаяГлубинаВложенности = 5;
      Объект.ВключатьНевидимыеЭлементы = Ложь;
      
      СтатусСбора = "Готов к работе";
  КонецПроцедуры
  ```
- **Критерий готовности**: При открытии формы все поля заполнены значениями по умолчанию

#### 2.2. Обработчик выбора файла `ПутьКФайлуВыгрузкиНачалоВыбора()`
- **Цель**: Диалог выбора файла для сохранения результата
- **Действия**:
  ```bsl
  &НаКлиенте
  Процедура ПутьКФайлуВыгрузкиНачалоВыбора(Элемент, ДанныеВыбора, ВыборДобавлением, СтандартнаяОбработка)
      СтандартнаяОбработка = Ложь;
      
      Диалог = Новый ДиалогВыбораФайла(РежимДиалогаВыбораФайла.Сохранение);
      Диалог.Заголовок = "Выберите файл для сохранения результата";
      Диалог.ПолноеИмяФайла = Объект.ПутьКФайлуВыгрузки;
      Диалог.Фильтр = "JSON файлы (*.json)|*.json";
      
      Если Диалог.Выбрать() Тогда
          Объект.ПутьКФайлуВыгрузки = Диалог.ПолноеИмяФайла;
      КонецЕсли;
  КонецПроцедуры
  ```
- **Критерий готовности**: Пользователь может выбрать файл через диалог

#### 2.3. Команда `СобратьКонтекстФормы()`
- **Цель**: Запуск процесса сбора контекста
- **Действия**:
  ```bsl
  &НаКлиенте
  Процедура СобратьКонтекстФормы(Команда)
      СобратьКонтекстФормыНаСервере();
  КонецПроцедуры
  
  &НаСервере
  Процедура СобратьКонтекстФормыНаСервере()
      СтатусСбора = "Выполняется сбор...";
      
      Попытка
          // 1. Инициализация подключения к TestClient
          ИнициализироватьТестируемуюФорму();
          
          // 2. Сбор информации о форме
          ДанныеФормы = СобратьИнформациюОФорме();
          
          // 3. Формирование JSON
          РезультатJSON = ПреобразоватьВJSON(ДанныеФормы);
          РезультатСбора = РезультатJSON;
          
          // 4. Сохранение в файл
          Если Не ПустаяСтрока(Объект.ПутьКФайлуВыгрузки) Тогда
              СохранитьВФайл(РезультатJSON);
              СтатусСбора = "Сбор завершен. Файл сохранен: " + Объект.ПутьКФайлуВыгрузки;
          Иначе
              СтатусСбора = "Сбор завершен. Файл не сохранен.";
          КонецЕсли;
          
      Исключение
          СтатусСбора = "Ошибка: " + ОписаниеОшибки();
          РезультатСбора = ПодробноеПредставлениеОшибки(ИнформацияОбОшибке());
      КонецПопытки;
  КонецПроцедуры
  ```
- **Критерий готовности**: При нажатии кнопки запускается процесс сбора

#### 2.4. Команда `Очистить()`
- **Цель**: Очистка результатов
- **Действия**:
  ```bsl
  &НаКлиенте
  Процедура Очистить(Команда)
      ОчиститьНаСервере();
  КонецПроцедуры
  
  &НаСервере
  Процедура ОчиститьНаСервере()
      РезультатСбора = "";
      СтатусСбора = "Готов к работе";
  КонецПроцедуры
  ```
- **Критерий готовности**: Результаты очищаются, статус сбрасывается

---

### 3. Функции сбора информации о форме 🔍

#### 3.1. Функция `СобратьИнформациюОФорме()`
- **Цель**: Создать структуру данных с информацией о форме
- **Действия**:
  ```bsl
  &НаСервере
  Функция СобратьИнформациюОФорме()
      // Создаем структуру для хранения данных
      ДанныеФормы = Новый Структура;
      ДанныеФормы.Вставить("form_name", "");
      ДанныеФормы.Вставить("form_type", "");
      ДанныеФормы.Вставить("title", "");
      ДанныеФормы.Вставить("elements", Новый Массив);
      ДанныеФормы.Вставить("commands", Новый Массив);
      ДанныеФормы.Вставить("tables", Новый Массив);
      ДанныеФормы.Вставить("metadata", Новый Структура);
      
      // Основная информация о форме
      ДанныеФормы.form_name = Строка(ТестируемаяФорма.Имя);
      ДанныеФормы.title = Строка(ТестируемаяФорма.Заголовок);
      ДанныеФормы.form_type = ОпределитьТипФормы(ТестируемаяФорма);
      
      // Метаданные
      ДанныеФормы.metadata.Вставить("collected_at", ТекущаяДата());
      ДанныеФормы.metadata.Вставить("collector_version", "1.0.0");
      ДанныеФормы.metadata.Вставить("max_depth", Объект.МаксимальнаяГлубинаВложенности);
      ДанныеФормы.metadata.Вставить("include_invisible", Объект.ВключатьНевидимыеЭлементы);
      
      // Сбор элементов формы
      ТекущаяГлубина = 0;
      СобратьЭлементыФормы(ДанныеФормы.elements);
      
      // Сбор табличных частей (если включено)
      Если Объект.ВключатьТабличныеЧасти Тогда
          СобратьТабличныеЧасти(ДанныеФормы.tables);
      КонецЕсли;
      
      // Сбор команд (если включено)
      Если Объект.ВключатьКоманды Тогда
          СобратьКоманды(ДанныеФормы.commands);
      КонецЕсли;
      
      Возврат ДанныеФормы;
  КонецФункции
  ```
- **Критерий готовности**: Функция возвращает заполненную структуру с данными

#### 3.2. Функция `ОпределитьТипФормы()`
- **Цель**: Определить тип формы (документ, справочник, обработка и т.д.)
- **Действия**:
  ```bsl
  &НаСервере
  Функция ОпределитьТипФормы(Форма)
      Попытка
          ИмяФормы = Строка(Форма.Имя);
          
          Если СтрНайти(ИмяФормы, "Документ.") > 0 Тогда
              Возврат "document_form";
          ИначеЕсли СтрНайти(ИмяФормы, "Справочник.") > 0 Тогда
              Возврат "catalog_form";
          ИначеЕсли СтрНайти(ИмяФормы, "Обработка.") > 0 Тогда
              Возврат "data_processor_form";
          ИначеЕсли СтрНайти(ИмяФормы, "Отчет.") > 0 Тогда
              Возврат "report_form";
          ИначеЕсли СтрНайти(ИмяФормы, "РегистрСведений.") > 0 Тогда
              Возврат "information_register_form";
          Иначе
              Возврат "other_form";
          КонецЕсли;
      Исключение
          Возврат "unknown";
      КонецПопытки;
  КонецФункции
  ```
- **Критерий готовности**: Корректно определяется тип для основных видов форм

---

### 4. Функции работы с элементами формы 📋

#### 4.1. Функция `СобратьЭлементыФормы()`
- **Цель**: Рекурсивный обход всех элементов формы
- **Сложность**: 🔴 Высокая (рекурсия, обработка разных типов элементов)
- **Действия**:
  ```bsl
  &НаСервере
  Процедура СобратьЭлементыФормы(МассивЭлементов, ТекущийРодитель = Неопределено, Путь = "")
      // Проверка ограничения глубины
      ТекущаяГлубина = ТекущаяГлубина + 1;
      Если ТекущаяГлубина > Объект.МаксимальнаяГлубинаВложенности Тогда
          ТекущаяГлубина = ТекущаяГлубина - 1;
          Возврат;
      КонецЕсли;
      
      Попытка
          // Получаем коллекцию элементов
          Если ТекущийРодитель = Неопределено Тогда
              Элементы = ТестируемаяФорма.ПолучитьЭлементы();
          Иначе
              Элементы = ТекущийРодитель.ПолучитьЭлементы();
          КонецЕсли;
          
          // Обход всех элементов
          Для Каждого Элемент Из Элементы Цикл
              // Пропуск невидимых элементов (если настройка выключена)
              Если Не Объект.ВключатьНевидимыеЭлементы Тогда
                  Попытка
                      Если Не Элемент.Видимость Тогда
                          Продолжить;
                      КонецЕсли;
                  Исключение
                      // У элемента может не быть свойства Видимость
                  КонецПопытки;
              КонецЕсли;
              
              // Создаем описание элемента
              ОписаниеЭлемента = СоздатьОписаниеЭлемента(Элемент, Путь);
              МассивЭлементов.Добавить(ОписаниеЭлемента);
              
              // Рекурсивный обход дочерних элементов для групп
              Если ЭлементЯвляетсяГруппой(Элемент) Тогда
                  НовыйПуть = ?(ПустаяСтрока(Путь), Элемент.Имя, Путь + "." + Элемент.Имя);
                  СобратьЭлементыФормы(ОписаниеЭлемента.children, Элемент, НовыйПуть);
              КонецЕсли;
          КонецЦикла;
          
      Исключение
          // Логирование ошибки
          ТекстОшибки = "Ошибка при сборе элементов: " + ОписаниеОшибки();
          СтатусСбора = СтатусСбора + Символы.ПС + ТекстОшибки;
      КонецПопытки;
      
      ТекущаяГлубина = ТекущаяГлубина - 1;
  КонецПроцедуры
  ```
- **Критерий готовности**: Все элементы формы собираются с учетом вложенности

#### 4.2. Функция `СоздатьОписаниеЭлемента()`
- **Цель**: Создать структуру с описанием одного элемента
- **Действия**:
  ```bsl
  &НаСервере
  Функция СоздатьОписаниеЭлемента(Элемент, Путь)
      // Создаем структуру для элемента
      Описание = Новый Структура;
      Описание.Вставить("name", ПолучитьСвойствоЭлемента(Элемент, "Имя"));
      Описание.Вставить("type", ОпределитьТипЭлемента(Элемент));
      Описание.Вставить("title", ПолучитьЗаголовокЭлемента(Элемент));
      Описание.Вставить("path", ?(ПустаяСтрока(Путь), Описание.name, Путь + "." + Описание.name));
      Описание.Вставить("visible", ПолучитьСвойствоВидимость(Элемент));
      Описание.Вставить("enabled", ПолучитьСвойствоДоступность(Элемент));
      Описание.Вставить("data_path", ПолучитьПутьКДанным(Элемент));
      
      // Дополнительные свойства в зависимости от типа
      ДобавитьСпецифичныеСвойства(Элемент, Описание);
      
      // Для групп - массив дочерних элементов
      Если ЭлементЯвляетсяГруппой(Элемент) Тогда
          Описание.Вставить("children", Новый Массив);
      КонецЕсли;
      
      Возврат Описание;
  КонецФункции
  ```
- **Критерий готовности**: Для любого элемента создается корректная структура

#### 4.3. Функция `ОпределитьТипЭлемента()`
- **Цель**: Определить тип элемента формы
- **Действия**:
  ```bsl
  &НаСервере
  Функция ОпределитьТипЭлемента(Элемент)
      Попытка
          ТипЭлемента = ТипЗнч(Элемент);
          ИмяТипа = Строка(ТипЭлемента);
          
          // Упрощенная типизация
          Если СтрНайти(ИмяТипа, "ПолеВвода") > 0 Тогда
              Возврат "input_field";
          ИначеЕсли СтрНайти(ИмяТипа, "Кнопка") > 0 Тогда
              Возврат "button";
          ИначеЕсли СтрНайти(ИмяТипа, "ТаблицаФормы") > 0 Тогда
              Возврат "table";
          ИначеЕсли СтрНайти(ИмяТипа, "Группа") > 0 Тогда
              Возврат "group";
          ИначеЕсли СтрНайти(ИмяТипа, "Надпись") > 0 Тогда
              Возврат "label";
          ИначеЕсли СтрНайти(ИмяТипа, "ПолеФлажка") > 0 Тогда
              Возврат "checkbox";
          ИначеЕсли СтрНайти(ИмяТипа, "ПолеПереключателя") > 0 Тогда
              Возврат "radio_button";
          ИначеЕсли СтрНайти(ИмяТипа, "ПолеКартинки") > 0 Тогда
              Возврат "picture_field";
          Иначе
              Возврат "other";
          КонецЕсли;
      Исключение
          Возврат "unknown";
      КонецПопытки;
  КонецФункции
  ```
- **Критерий готовности**: Корректно определяются основные типы элементов

#### 4.4. Вспомогательные функции получения свойств
- **Цель**: Безопасно получать свойства элементов с обработкой исключений
- **Функции**:
  ```bsl
  &НаСервере
  Функция ПолучитьСвойствоЭлемента(Элемент, ИмяСвойства)
      Попытка
          Возврат Строка(Элемент[ИмяСвойства]);
      Исключение
          Возврат "";
      КонецПопытки;
  КонецФункции
  
  &НаСервере
  Функция ПолучитьЗаголовокЭлемента(Элемент)
      Попытка
          Возврат Строка(Элемент.Заголовок);
      Исключение
          Возврат "";
      КонецПопытки;
  КонецФункции
  
  &НаСервере
  Функция ПолучитьСвойствоВидимость(Элемент)
      Попытка
          Возврат Элемент.Видимость;
      Исключение
          Возврат Истина;
      КонецПопытки;
  КонецФункции
  
  &НаСервере
  Функция ПолучитьСвойствоДоступность(Элемент)
      Попытка
          Возврат Элемент.Доступность;
      Исключение
          Возврат Истина;
      КонецПопытки;
  КонецФункции
  
  &НаСервере
  Функция ПолучитьПутьКДанным(Элемент)
      Попытка
          Возврат Строка(Элемент.ПутьКДанным);
      Исключение
          Возврат "";
      КонецПопытки;
  КонецФункции
  
  &НаСервере
  Функция ЭлементЯвляетсяГруппой(Элемент)
      Попытка
          // Проверяем наличие метода получения дочерних элементов
          Элемент.ПолучитьЭлементы();
          Возврат Истина;
      Исключение
          Возврат Ложь;
      КонецПопытки;
  КонецФункции
  ```
- **Критерий готовности**: Все функции корректно обрабатывают отсутствие свойств

#### 4.5. Функция `ДобавитьСпецифичныеСвойства()`
- **Цель**: Добавить специфичные свойства в зависимости от типа элемента
- **Действия**:
  ```bsl
  &НаСервере
  Процедура ДобавитьСпецифичныеСвойства(Элемент, Описание)
      ТипЭлемента = Описание.type;
      
      Если ТипЭлемента = "input_field" Тогда
          Попытка
              Описание.Вставить("read_only", Элемент.ТолькоПросмотр);
          Исключение
          КонецПопытки;
          
          Попытка
              Описание.Вставить("input_hint", Строка(Элемент.ПодсказкаВвода));
          Исключение
          КонецПопытки;
          
      ИначеЕсли ТипЭлемента = "button" Тогда
          Попытка
              Описание.Вставить("command", Строка(Элемент.ИмяКоманды));
          Исключение
          КонецПопытки;
          
      ИначеЕсли ТипЭлемента = "table" Тогда
          Попытка
              Описание.Вставить("selection_mode", Строка(Элемент.РежимВыделения));
          Исключение
          КонецПопытки;
          
          Попытка
              Описание.Вставить("auto_add", Элемент.АвтоДобавлениеПриВводе);
          Исключение
          КонецПопытки;
      КонецЕсли;
  КонецПроцедуры
  ```
- **Критерий готовности**: Специфичные свойства добавляются для основных типов

---

### 5. Функции работы с табличными частями и командами 📊

#### 5.1. Функция `СобратьТабличныеЧасти()` (заглушка для этапа 1)
- **Цель**: Собрать информацию о табличных частях формы
- **Действия** (минимальная реализация):
  ```bsl
  &НаСервере
  Процедура СобратьТабличныеЧасти(МассивТаблиц)
      // TODO: Полная реализация будет на этапе 2
      // Для этапа 1 - минимальный функционал
      
      Попытка
          Элементы = ТестируемаяФорма.ПолучитьЭлементы();
          
          Для Каждого Элемент Из Элементы Цикл
              Если ОпределитьТипЭлемента(Элемент) = "table" Тогда
                  ОписаниеТаблицы = Новый Структура;
                  ОписаниеТаблицы.Вставить("name", ПолучитьСвойствоЭлемента(Элемент, "Имя"));
                  ОписаниеТаблицы.Вставить("title", ПолучитьЗаголовокЭлемента(Элемент));
                  ОписаниеТаблицы.Вставить("data_path", ПолучитьПутьКДанным(Элемент));
                  
                  МассивТаблиц.Добавить(ОписаниеТаблицы);
              КонецЕсли;
          КонецЦикла;
          
      Исключение
          // Игнорируем ошибки на этапе 1
      КонецПопытки;
  КонецПроцедуры
  ```
- **Критерий готовности**: Функция не падает, собирает базовую информацию о таблицах

#### 5.2. Функция `СобратьКоманды()` (заглушка для этапа 1)
- **Цель**: Собрать информацию о командах формы
- **Действия** (минимальная реализация):
  ```bsl
  &НаСервере
  Процедура СобратьКоманды(МассивКоманд)
      // TODO: Полная реализация будет на этапе 2
      // Для этапа 1 - пропускаем сбор команд
      Возврат;
  КонецПроцедуры
  ```
- **Критерий готовности**: Функция не падает

---

### 6. Функции формирования и сохранения результата 💾

#### 6.1. Функция `ПреобразоватьВJSON()`
- **Цель**: Преобразовать структуру данных в JSON
- **Действия**:
  ```bsl
  &НаСервере
  Функция ПреобразоватьВJSON(Данные)
      Попытка
          ЗаписьJSON = Новый ЗаписьJSON;
          ЗаписьJSON.УстановитьСтроку(Новый ПараметрыЗаписиJSON(, Символы.Таб));
          ЗаписатьJSON(ЗаписьJSON, Данные);
          
          Возврат ЗаписьJSON.Закрыть();
      Исключение
          ВызватьИсключение "Ошибка при преобразовании в JSON: " + ОписаниеОшибки();
      КонецПопытки;
  КонецФункции
  ```
- **Критерий готовности**: JSON корректно формируется и форматируется

#### 6.2. Функция `СохранитьВФайл()`
- **Цель**: Сохранить JSON в файл
- **Действия**:
  ```bsl
  &НаСервере
  Процедура СохранитьВФайл(ТекстJSON)
      Попытка
          ЗаписьТекста = Новый ЗаписьТекста(Объект.ПутьКФайлуВыгрузки, КодировкаТекста.UTF8);
          ЗаписьТекста.Записать(ТекстJSON);
          ЗаписьТекста.Закрыть();
      Исключение
          ВызватьИсключение "Ошибка при сохранении файла: " + ОписаниеОшибки();
      КонецПопытки;
  КонецПроцедуры
  ```
- **Критерий готовности**: Файл успешно сохраняется с корректной кодировкой UTF-8

---

## Последовательность реализации

### Фаза A: Подготовка (Задачи 1.1 - 1.3)
1. Изучить API TestClient
2. Добавить переменные модуля
3. Реализовать `ИнициализироватьТестируемуюФорму()`

**Результат**: Есть подключение к TestClient и получение активной формы

### Фаза B: Базовый каркас (Задачи 2.1 - 2.4)
1. Реализовать `ПриСозданииНаСервере()`
2. Реализовать диалог выбора файла
3. Создать каркас команды `СобратьКонтекстФормы()`
4. Реализовать команду `Очистить()`

**Результат**: Форма работает, можно запустить процесс сбора

### Фаза C: Основной функционал (Задачи 3.1 - 3.2)
1. Реализовать `СобратьИнформациюОФорме()`
2. Реализовать `ОпределитьТипФормы()`

**Результат**: Создается базовая структура с данными формы

### Фаза D: Сбор элементов (Задачи 4.1 - 4.5)
1. Реализовать все вспомогательные функции получения свойств (4.4)
2. Реализовать `ОпределитьТипЭлемента()` (4.3)
3. Реализовать `СоздатьОписаниеЭлемента()` (4.2)
4. Реализовать `ДобавитьСпецифичныеСвойства()` (4.5)
5. Реализовать `СобратьЭлементыФормы()` (4.1)

**Результат**: Все элементы формы собираются с полной информацией

### Фаза E: Завершение (Задачи 5.1 - 6.2)
1. Создать заглушки для табличных частей и команд (5.1, 5.2)
2. Реализовать `ПреобразоватьВJSON()` (6.1)
3. Реализовать `СохранитьВФайл()` (6.2)

**Результат**: Результат корректно сохраняется в JSON файл

---

## Критерии завершения этапа 1

### Функциональные критерии:
- ✅ Обработка успешно подключается к Vanessa Automation TestClient
- ✅ Получает активную форму из клиента тестирования
- ✅ Собирает информацию о типе формы и её заголовке
- ✅ Рекурсивно обходит все элементы формы с учетом вложенности
- ✅ Для каждого элемента собирает: имя, тип, заголовок, путь, видимость, доступность, путь к данным
- ✅ Формирует корректный JSON с собранной информацией
- ✅ Сохраняет JSON в файл с кодировкой UTF-8
- ✅ Отображает статус операции в форме

### Технические критерии:
- ✅ Код соответствует стандартам 1С
- ✅ Все функции имеют обработку исключений
- ✅ Код разбит на логические области (#Область)
- ✅ Есть комментарии для сложных участков
- ✅ Переменные и функции имеют понятные имена

### Критерии качества:
- ✅ Обработка не падает при отсутствии активной формы
- ✅ Корректно обрабатываются элементы без некоторых свойств
- ✅ Не зависает на глубоко вложенных структурах (ограничение глубины)
- ✅ Выдает понятные сообщения об ошибках
- ✅ JSON имеет читаемый формат (с отступами)

---

## Тестирование

### Тестовые сценарии:

#### Тест 1: Простая форма
- **Форма**: Форма элемента справочника (без табличных частей)
- **Ожидаемый результат**: JSON со всеми полями формы

#### Тест 2: Форма с группами
- **Форма**: Форма документа с группировкой полей
- **Ожидаемый результат**: JSON с правильной вложенностью групп

#### Тест 3: Форма с таблицей
- **Форма**: Форма документа с табличной частью
- **Ожидаемый результат**: JSON с информацией о таблице

#### Тест 4: Невидимые элементы
- **Настройка**: `ВключатьНевидимыеЭлементы = Ложь`
- **Ожидаемый результат**: Невидимые элементы не попадают в JSON

#### Тест 5: Ограничение глубины
- **Настройка**: `МаксимальнаяГлубинаВложенности = 2`
- **Форма**: Форма с глубокой вложенностью
- **Ожидаемый результат**: Вложенность ограничена настройкой

---

## Риски и сложности

### 🔴 Высокий риск:
1. **API TestClient может отличаться в разных версиях Vanessa Automation**
   - Решение: Предусмотреть проверку версии и адаптацию кода

2. **Разные версии платформы 1С имеют разные свойства элементов**
   - Решение: Везде использовать `Попытка...Исключение`

### 🟡 Средний риск:
1. **Производительность при большом количестве элементов**
   - Решение: Ограничение глубины, возможность отключения невидимых элементов

2. **Кодировка при сохранении файла**
   - Решение: Явно указывать UTF-8

### 🟢 Низкий риск:
1. **Конфликты имен элементов**
   - Решение: Использовать полные пути (path)

---

## Следующие этапы после завершения

После успешного завершения этапа 1:

1. **Этап 2**: Расширенный функционал
   - Полный сбор информации о табличных частях (колонки, их свойства)
   - Детальный сбор команд формы
   - Опциональные снимки экрана

2. **Этап 3**: Python-инструменты
   - CLI-скрипт для запуска обработки
   - Форматирование JSON → Markdown (AI-оптимизированный формат)

3. **Этап 4**: Интеграция
   - Интеграция с существующим инструментарием
   - Документация и примеры использования
   - Рефакторинг и оптимизация

---

## Ресурсы и ссылки

- Спецификация обработки: [`PROCESSING_STRUCTURE.md`](PROCESSING_STRUCTURE.md)
- Архитектура: [`docs/architecture/form-context-collector.md`](../../docs/architecture/form-context-collector.md)
- Пример Исследователя Форм: [`upload/vanessa-automation-single/vanessa-automation-single/Forms/ИсследовательФормы/Ext/Form/Module.bsl`](../../upload/vanessa-automation-single/vanessa-automation-single/Forms/ИсследовательФормы/Ext/Form/Module.bsl)
- Исходники обработки: [`src/FormContextCollector/Forms/Форма/Ext/Form/Module.bsl`](src/FormContextCollector/Forms/Форма/Ext/Form/Module.bsl)