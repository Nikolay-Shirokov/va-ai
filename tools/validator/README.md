# Валидатор сценариев Vanessa Automation

## Описание

Скрипт `validate_scenario.py` автоматически проверяет сценарии Vanessa Automation на:

1. ✅ **Наличие всех шагов в библиотеке** - каждый шаг проверяется на существование
2. ✅ **Корректность синтаксиса Gherkin** - правильные ключевые слова, кавычки
3. ✅ **Обязательные элементы** - кодировка, язык, блок Функционал
4. ✅ **Правильность переменных** - определены ли переменные перед использованием
5. ✅ **Предложения исправлений** - для каждой ошибки предлагаются похожие шаги

## Установка

### Требования

- Python 3.6+
- Стандартная библиотека Python (ничего дополнительно устанавливать не нужно!)

### Скачивание

```bash
# Скрипт готов к использованию, просто сделайте его исполняемым
chmod +x validate_scenario.py
```

## Использование

### Базовое использование

```bash
python validate_scenario.py scenario.feature
```

### С указанием пути к библиотеке

```bash
python validate_scenario.py scenario.feature --library БиблиотекаШагов.json
```

### Подробный вывод

```bash
python validate_scenario.py scenario.feature --verbose
```

### Формат для AI-ассистента

```bash
python validate_scenario.py scenario.feature --ai-format
```

### Все опции вместе

```bash
python validate_scenario.py scenario.feature \
    --library /path/to/БиблиотекаШагов.json \
    --verbose \
    --ai-format
```

## Опции командной строки

| Опция | Сокращение | Описание |
|-------|-----------|----------|
| `--library PATH` | `-l` | Путь к файлу библиотеки шагов (по умолчанию: БиблиотекаШагов.json) |
| `--verbose` | `-v` | Подробный вывод с текстом каждого шага |
| `--ai-format` | | Вывод рекомендаций в формате для AI-ассистента |
| `--help` | `-h` | Показать справку |

## Примеры вывода

### Успешная валидация

```
================================================================================
ОТЧЕТ О ВАЛИДАЦИИ СЦЕНАРИЯ
================================================================================

📊 СТАТИСТИКА:
  Функционалов: 1
  Сценариев: 2
  Всего шагов: 15
  ✓ Валидных шагов: 15
  ✗ Невалидных шагов: 0

  [████████████████████████████████████████] 100.0%

✓ Ошибок не найдено!

================================================================================
✓ СЦЕНАРИЙ ВАЛИДЕН И ГОТОВ К ЗАПУСКУ!
================================================================================
```

### Валидация с ошибками

```
================================================================================
ОТЧЕТ О ВАЛИДАЦИИ СЦЕНАРИЯ
================================================================================

📊 СТАТИСТИКА:
  Функционалов: 1
  Сценариев: 1
  Всего шагов: 8
  ✓ Валидных шагов: 4
  ✗ Невалидных шагов: 4

  [████████████████████░░░░░░░░░░░░░░░░░░░░] 50.0%

❌ ОШИБКИ (4):

1. Строка 8: Шаг не найден в библиотеке
   Шаг: Когда я нажимаю на кнопку 'Создать'
   💡 Рекомендация: Возможно, вы имели в виду один из этих шагов
   Похожие шаги из библиотеки:
      1. И я нажимаю на кнопку "Заголовок кнопки"
      2. И я нажимаю на кнопку с именем "ИмяКнопки"

2. Строка 8: Использованы одинарные кавычки вместо двойных
   💡 Рекомендация: Замените одинарные кавычки ' на двойные "

...

⚠️  ПРЕДУПРЕЖДЕНИЯ (1):

1. Строка 10: Переменная "$Контрагент$" используется, но не определена
   💡 Рекомендация: Добавьте шаг для определения переменной "Контрагент"

================================================================================
✗ ТРЕБУЕТСЯ ИСПРАВЛЕНИЕ ОШИБОК
================================================================================
```

### Формат для AI (--ai-format)

```
📋 РЕКОМЕНДАЦИИ ДЛЯ AI-АССИСТЕНТА:

Обнаружены следующие проблемы, которые нужно исправить:

Шаги, не найденные в библиотеке:

1. Строка 8:
   ❌ Неверный шаг: Когда я нажимаю на кнопку 'Создать'
   ✅ Замените на один из этих шагов:
      1) И я нажимаю на кнопку "Заголовок кнопки"
      2) И я нажимаю на кнопку с именем "ИмяКнопки"

Другие проблемы:

• Отсутствует строка с кодировкой (строка 1)
  Решение: Добавьте в начало файла: # encoding: utf-8
```

## Интеграция с AI-ассистентом

### Workflow с валидацией

```
1. AI генерирует сценарий → scenario.feature
                              ↓
2. Валидатор проверяет     → python validate_scenario.py scenario.feature --ai-format
                              ↓
3. Результат возвращается AI → Список ошибок с рекомендациями
                              ↓
4. AI исправляет ошибки    → Обновленный scenario.feature
                              ↓
5. Повторная валидация     → ✓ Готово к запуску!
```

### Промпт для AI с валидацией

```
Ты - эксперт по Vanessa Automation.

АЛГОРИТМ РАБОТЫ:
1. Сгенерируй сценарий по запросу пользователя
2. Я запущу валидатор: python validate_scenario.py scenario.feature --ai-format
3. Если есть ошибки - исправь их согласно рекомендациям
4. Повторяй пока валидация не пройдет успешно

ВАЖНО:
- Используй ТОЛЬКО шаги из библиотеки
- Следуй рекомендациям валидатора
- Не выдумывай новые шаги
```

## Что проверяет валидатор

### 1. Заголовки файла

❌ **Ошибка:**
```gherkin
Функционал: Тест
```

✅ **Правильно:**
```gherkin
# encoding: utf-8
# language: ru

Функционал: Тест
```

### 2. Синтаксис кавычек

❌ **Ошибка:**
```gherkin
И в поле 'Наименование' я ввожу текст 'Текст'
```

✅ **Правильно:**
```gherkin
И в поле "Наименование" я ввожу текст "Текст"
```

### 3. Существование шагов

❌ **Ошибка:**
```gherkin
И я выбираю товар из списка "Номенклатура"
```

✅ **Правильно (предложение валидатора):**
```gherkin
И из выпадающего списка "Номенклатура" я выбираю по строке "Товар 1"
```

### 4. Использование переменных

❌ **Ошибка:**
```gherkin
И в поле "Покупатель" я ввожу текст "$Контрагент$"
# Переменная не определена!
```

✅ **Правильно:**
```gherkin
И я запоминаю значение поля "Контрагент" в переменную "Контрагент"
И в поле "Покупатель" я ввожу текст "$Контрагент$"
```

### 5. Структура файла

❌ **Ошибка:**
```gherkin
# encoding: utf-8

Сценарий: Тест
    Дано ...
```

✅ **Правильно:**
```gherkin
# encoding: utf-8
# language: ru

Функционал: Название
    Описание

Сценарий: Тест
    Дано ...
```

## Как работает поиск похожих шагов

Валидатор использует алгоритм нечеткого сравнения:

1. **Нормализация** - удаляет параметры и приводит к общему виду
2. **Сравнение** - вычисляет схожесть (Sequence Matcher)
3. **Ранжирование** - сортирует по убыванию схожести
4. **Топ-5** - возвращает лучшие варианты

**Пример:**

Ваш шаг: `И я нажимаю кнопку "Записать"`

Нормализация: `я нажимаю кнопку "{}"`

Похожие из библиотеки:
- `И я нажимаю на кнопку "{}"` (95% схожесть)
- `И я нажимаю на гиперссылку "{}"` (85% схожесть)
- `И я нажимаю кнопку выбора у поля "{}"` (80% схожесть)

## Коды возврата

- `0` - валидация успешна, ошибок нет
- `1` - найдены ошибки, требуется исправление

Это позволяет использовать скрипт в CI/CD:

```bash
#!/bin/bash
if python validate_scenario.py scenario.feature; then
    echo "Сценарий валиден, запускаем тесты..."
    vanessa-automation.exe scenario.feature
else
    echo "Ошибки в сценарии, исправьте перед запуском"
    exit 1
fi
```

## Интеграция в CI/CD

### GitLab CI

```yaml
validate_scenarios:
  stage: test
  script:
    - python validate_scenario.py features/*.feature
  artifacts:
    when: on_failure
    paths:
      - validation_report.txt
```

### GitHub Actions

```yaml
- name: Validate Scenarios
  run: |
    for file in features/*.feature; do
      python validate_scenario.py "$file" || exit 1
    done
```

## Расширение функционала

### Добавление своих проверок

Скрипт легко расширяется. Например, добавим проверку на наличие тегов:

```python
def _check_tags(self, lines: List[str]):
    """Проверка наличия тегов"""
    has_tags = False
    
    for line in lines:
        if line.strip().startswith('@'):
            has_tags = True
            break
    
    if not has_tags:
        self.warnings.append({
            'line': 0,
            'type': 'tags',
            'message': 'Отсутствуют теги для фильтрации',
            'suggestion': 'Добавьте теги @Smoke, @Critical и т.д.'
        })
```

## FAQ

**Q: Как часто запускать валидацию?**
A: Перед каждым коммитом в Git и перед запуском тестов.

**Q: Можно ли валидировать несколько файлов?**
A: Да, используйте цикл:
```bash
for file in features/*.feature; do
    python validate_scenario.py "$file"
done
```

**Q: Что делать, если валидатор не нашел похожий шаг?**
A: Проверьте библиотеку шагов вручную или используйте компактный справочник VANESSA_STEPS_COMPACT.md

**Q: Можно ли игнорировать предупреждения?**
A: Да, предупреждения не блокируют выполнение (код возврата 0), только ошибки.

**Q: Поддерживается ли английский Gherkin?**
A: Сейчас только русский. Для английского нужно добавить KEYWORDS = ['Given', 'When', 'Then', 'And'].

## Поддержка

- 🐛 Нашли баг? Создайте issue
- 💡 Есть идея? Предложите улучшение
- 📖 Нужна помощь? Смотрите документацию Vanessa Automation

## Лицензия

MIT License - свободное использование

---

**Версия:** 1.0  
**Дата:** Ноябрь 2025  
**Совместимость:** Vanessa Automation 1.2.042.x+
