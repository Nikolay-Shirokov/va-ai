# Валидатор сценариев - Быстрый старт

## 🎯 Что это?

Скрипт `validate.py` проверяет сценарии Vanessa Automation и выдает рекомендации AI по исправлению ошибок.

## ⚡ Быстрый запуск

```bash
# Базовое использование
python tools/validator/validate.py scenario.feature

# С рекомендациями для AI
python tools/validator/validate.py scenario.feature --ai-format
```

## 📋 Что проверяется

✅ Все шаги есть в библиотеке  
✅ Правильный синтаксис Gherkin  
✅ Обязательные заголовки (encoding, language)  
✅ Корректность кавычек (только двойные ")  
✅ Определение переменных перед использованием  
✅ Наличие блока Функционал  

## 💡 Пример вывода

### Сценарий с ошибками:

```gherkin
# language: ru

Сценарий: Тест
    Когда я нажимаю на кнопку 'Создать'
    И я выбираю товар из списка
```

### Вывод валидатора:

```
❌ ОШИБКИ (3):

1. Строка 1: Отсутствует строка с кодировкой
   💡 Рекомендация: Добавьте в начало файла: # encoding: utf-8

2. Строка 4: Шаг не найден в библиотеке
   ❌ Неверный шаг: Когда я нажимаю на кнопку 'Создать'
   ✅ Замените на один из этих шагов:
      1) И я нажимаю на кнопку "Заголовок кнопки"
      2) И я нажимаю на кнопку с именем "ИмяКнопки"

3. Строка 4: Использованы одинарные кавычки
   💡 Рекомендация: Замените ' на "
```

## 🤖 Интеграция с AI

### Промпт для AI-ассистента:

```
После генерации сценария ОБЯЗАТЕЛЬНО:

1. Попроси пользователя запустить:
   python tools/validator/validate.py scenario.feature --ai-format

2. Дождись результатов валидации

3. Если есть ошибки - исправь их согласно рекомендациям

4. Повторяй пока все ошибки не будут исправлены
```

### Workflow:

```
AI генерирует сценарий
        ↓
Пользователь запускает валидатор
        ↓
Валидатор выдает рекомендации
        ↓
AI исправляет по рекомендациям
        ↓
✅ Сценарий готов!
```

## 📂 Файлы

- **validate.py** - основной скрипт
- **README.md** - полная документация

## 🔧 Опции

```bash
--library PATH    # Путь к БиблиотекаШагов.json
--verbose         # Подробный вывод
--ai-format       # Рекомендации для AI
```

## ✅ Преимущества

1. **Ускоряет разработку** - сразу видны все ошибки
2. **Предлагает исправления** - топ-5 похожих шагов
3. **Обучает AI** - четкие рекомендации что исправить
4. **Без зависимостей** - только Python 3.6+
5. **CI/CD ready** - коды возврата 0/1

## 📖 Подробнее

См. **README.md** для:
- Детальной документации
- Примеров использования
- Интеграции в CI/CD
- FAQ

---

**Используйте валидатор перед каждым запуском сценариев!**
