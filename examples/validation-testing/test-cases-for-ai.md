# Тестовые сценарии для проверки AI-валидатора

> **Версия:** 1.0  
> **Цель:** Набор тестовых кейсов для проверки правильности работы AI с валидатором  
> **Использование:** Для тестирования и обучения AI-ассистентов

---

## 📚 Содержание

1. [Уровень 1: Auto-fix ошибки](#уровень-1-auto-fix-ошибки)
2. [Уровень 2: Semantic check - успешные случаи](#уровень-2-semantic-check---успешные-случаи)
3. [Уровень 3: Semantic check - опасные случаи](#уровень-3-semantic-check---опасные-случаи)
4. [Уровень 4: Критические случаи](#уровень-4-критические-случаи)
5. [Уровень 5: Зацикливание](#уровень-5-зацикливание)

---

## Уровень 1: Auto-fix ошибки

### Тест 1.1: Одинарные кавычки

**Входной сценарий:**
```gherkin
# language: ru

Функционал: Создание контрагента

Сценарий: Создание нового контрагента
    Дано Я открываю навигационную ссылку 'e1cib/list/Справочник.Контрагенты'
    Когда я нажимаю на кнопку 'Создать'
    Тогда открылось окно 'Контрагент (создание)'
```

**Ожидаемое поведение AI:**
- ✅ Автоматически заменить `'` на `"`
- ✅ Повторно запустить валидатор
- ✅ Сообщить пользователю: "Исправлены одинарные кавычки на двойные"

**Правильный результат:**
```gherkin
# language: ru

Функционал: Создание контрагента

Сценарий: Создание нового контрагента
    Дано Я открываю навигационную ссылку "e1cib/list/Справочник.Контрагенты"
    Когда я нажимаю на кнопку "Создать"
    Тогда открылось окно "Контрагент (создание)"
```

---

### Тест 1.2: Отсутствующие заголовки

**Входной сценарий:**
```gherkin
Функционал: Создание контрагента

Сценарий: Создание нового контрагента
    Дано Я открываю навигационную ссылку "e1cib/list/Справочник.Контрагенты"
```

**Ожидаемое поведение AI:**
- ✅ Добавить `# encoding: utf-8` в начало
- ✅ Повторно запустить валидатор
- ✅ Объяснить: "Добавлена строка кодировки"

**Правильный результат:**
```gherkin
# encoding: utf-8
# language: ru

Функционал: Создание контрагента

Сценарий: Создание нового контрагента
    Дано Я открываю навигационную ссылку "e1cib/list/Справочник.Контрагенты"
```

---

## Уровень 2: Semantic check - успешные случаи

### Тест 2.1: Добавление предлога

**Входной сценарий:**
```gherkin
# encoding: utf-8
# language: ru

Функционал: Создание документа

Сценарий: Создание заказа
    Когда я нажимаю кнопку "Записать"
```

**Рекомендация валидатора:**
```yaml
suggestions:
  - text: 'И я нажимаю на кнопку "ЗаголовокКнопки"'
    similarity: 0.95
    parsed:
      action: "нажимаю"
      element: "кнопка"
    semantic_match:
      action: true
      element: true
      context: true
```

**Ожидаемое поведение AI:**
```
✅ Семантическая проверка:
- Действие: нажимаю → нажимаю ✅
- Элемент: кнопка → кнопка ✅
- Контекст: - → - ✅
- Confidence: HIGH

✅ Применить с сохранением параметра "Записать"
```

**Правильный результат:**
```gherkin
# encoding: utf-8
# language: ru

Функционал: Создание документа

Сценарий: Создание заказа
    Когда я нажимаю на кнопку "Записать"
```

**Сообщение пользователю:**
```
✅ Исправлен шаг на строке 6:
   Было: И я нажимаю кнопку "Записать"
   Стало: И я нажимаю на кнопку "Записать"
   
💬 Добавлен предлог "на" согласно синтаксису библиотеки Vanessa.
```

---

### Тест 2.2: Изменение порядка слов

**Входной сценарий:**
```gherkin
# encoding: utf-8
# language: ru

Функционал: Ввод данных

Сценарий: Заполнение формы
    И я ввожу в поле "Наименование" текст "Тест"
```

**Рекомендация валидатора:**
```yaml
suggestions:
  - text: 'И в поле "ИмяПоля" я ввожу текст "Значение"'
    similarity: 0.92
    semantic_match:
      action: true   # ввожу → ввожу
      element: true  # поле → поле
```

**Ожидаемое поведение AI:**
```
✅ Проверка:
- Действие совпадает ✅
- Элемент совпадает ✅
- Только порядок слов отличается

✅ Применить с сохранением параметров
```

**Правильный результат:**
```gherkin
# encoding: utf-8
# language: ru

Функционал: Ввод данных

Сценарий: Заполнение формы
    И в поле "Наименование" я ввожу текст "Тест"
```

---

## Уровень 3: Semantic check - опасные случаи

### Тест 3.1: ❌ Кнопка vs Гиперссылка

**Входной сценарий:**
```gherkin
# encoding: utf-8
# language: ru

Функционал: Навигация

Сценарий: Переход к отчету
    И я нажимаю кнопку "Печать"
```

**Рекомендации валидатора:**
```yaml
suggestions:
  1:
    text: 'И я нажимаю на кнопку "ЗаголовокКнопки"'
    similarity: 0.94
    semantic_match:
      action: true
      element: true  # кнопка → кнопка ✅
    confidence: "high"
    
  2:
    text: 'И я нажимаю на гиперссылку "ТекстГиперссылки"'
    similarity: 0.88
    semantic_match:
      action: true
      element: false  # кнопка → гиперссылка ❌
    warnings: ["Different UI element type: 'кнопка' vs 'гиперссылка'"]
    confidence: "low"
```

**Ожидаемое поведение AI:**
```
Анализ рекомендации #1:
✅ Действие: совпадает
✅ Элемент: совпадает
✅ Confidence: HIGH
→ Применить

Анализ рекомендации #2:
✅ Действие: совпадает
❌ Элемент: кнопка ≠ гиперссылка
⚠️ Warning: Different UI element type
❌ Confidence: LOW
→ НЕ применять
```

**Правильный результат:**
```gherkin
# encoding: utf-8
# language: ru

Функционал: Навигация

Сценарий: Переход к отчету
    И я нажимаю на кнопку "Печать"
```

**Сообщение пользователю:**
```
✅ Применена рекомендация #1

💬 Рекомендация #2 отклонена из-за несовпадения типа UI элемента:
   - Оригинал: кнопка
   - Рекомендация: гиперссылка
   
Это разные элементы интерфейса, замена может нарушить логику теста.
```

---

### Тест 3.2: ❌ Ввод vs Выбор из списка

**Входной сценарий:**
```gherkin
# encoding: utf-8
# language: ru

Функционал: Заполнение справочника

Сценарий: Указание контрагента
    И в поле "Контрагент" я ввожу текст "ООО Тест"
```

**Рекомендации валидатора:**
```yaml
suggestions:
  1:
    text: 'И в поле "ИмяПоля" я ввожу текст "Значение"'
    similarity: 0.96
    semantic_match:
      action: true   # ввожу → ввожу ✅
      element: true  # поле → поле ✅
    confidence: "high"
    
  2:
    text: 'И из выпадающего списка "ИмяСписка" я выбираю по строке "Значение"'
    similarity: 0.78
    semantic_match:
      action: false  # ввожу → выбираю ❌
      element: false # поле → выпадающий список ❌
    warnings: ["Different action", "Different element type"]
    confidence: "low"
```

**Ожидаемое поведение AI:**
```
❌ Рекомендация #2:
- Действие: ввожу ≠ выбираю (КРИТИЧНО!)
- Элемент: поле ≠ выпадающий список

Это ПРИНЦИПИАЛЬНО разные действия:
- "ввожу" = набор текста
- "выбираю из списка" = клик по элементу

→ НЕ применять рекомендацию #2
→ Использовать рекомендацию #1
```

**Правильный результат:**
```gherkin
# encoding: utf-8
# language: ru

Функционал: Заполнение справочника

Сценарий: Указание контрагента
    И в поле "Контрагент" я ввожу текст "ООО Тест"
```

---

### Тест 3.3: ⚠️ Спорный случай - нужен пользователь

**Входной сценарий:**
```gherkin
# encoding: utf-8
# language: ru

Функционал: Выбор номенклатуры

Сценарий: Указание товара
    И в поле "Номенклатура" я ввожу текст "Товар 1"
```

**Рекомендации валидатора:**
```yaml
suggestions:
  1:
    text: 'И в поле "ИмяПоля" я ввожу текст "Значение"'
    similarity: 0.95
    semantic_match:
      action: true
      element: true
    confidence: "high"
    
  2:
    text: 'И из выпадающего списка "ИмяСписка" я выбираю по строке "Значение"'
    similarity: 0.82
    semantic_match:
      action: false
      element: false
    confidence: "medium"
```

**Ожидаемое поведение AI:**
```
⚠️ Неоднозначная ситуация:

Поле "Номенклатура" в 1С может работать двумя способами:
1. Как обычное поле (можно набирать текст)
2. Как поле с выбором (нужно выбирать из списка)

Рекомендация #1: ввод текста (сохраняет текущую логику)
Рекомендация #2: выбор из списка (изменяет логику)

→ Применить рекомендацию #1 (безопаснее)
→ Спросить пользователя для подтверждения
```

**Сообщение пользователю:**
```
✅ Применена рекомендация #1: ввод текста в поле

💬 Обратите внимание:
Поле "Номенклатура" исправлено как поле ввода.

Если в вашей конфигурации это поле с выбором из справочника,
возможно, лучше использовать:
  И из выпадающего списка "Номенклатура" я выбираю по строке "Товар 1"

Текущий вариант работает? [Да / Нужно изменить]
```

---

## Уровень 4: Критические случаи

### Тест 4.1: Нет подходящих рекомендаций

**Входной сценарий:**
```gherkin
# encoding: utf-8
# language: ru

Функционал: Специфичная функциональность

Сценарий: Использование кастомного действия
    И я выполняю специальную операцию "Синхронизация" для объекта "Контрагенты"
```

**Результат валидатора:**
```yaml
errors:
  - line: 6
    type: "step_not_found"
    severity: "critical"
    step: 'И я выполняю специальную операцию "Синхронизация" для объекта "Контрагенты"'
    suggestions: []  # Нет рекомендаций (similarity < 70%)
    message: "Шаг не найден в библиотеке, похожих шагов не обнаружено"
```

**Ожидаемое поведение AI:**
```
❌ Критическая ситуация:
- Шаг не найден
- Нет рекомендаций (схожесть < 70%)

Возможные причины:
1. Это специфичный шаг вашей конфигурации (расширение библиотеки)
2. Неправильный подход к тестированию
3. Опечатка в описании действия

→ Обратиться к пользователю
```

**Сообщение пользователю:**
```
🤝 Требуется ваша помощь

**Строка 6:**
```gherkin
И я выполняю специальную операцию "Синхронизация" для объекта "Контрагенты"
```

❌ Проблема: Шаг не найден в библиотеке Vanessa Automation.
❌ Похожих шагов не найдено (схожесть < 70%).

**Возможные решения:**

1. 🆕 **Добавить кастомный шаг** в вашу конфигурацию:
   - Создать epf/erf с реализацией шага
   - Добавить в библиотеку шагов проекта

2. 🔄 **Использовать другой подход:**
   - Заменить на стандартные шаги Vanessa
   - Например: открыть форму → нажать кнопку → проверить результат

3. ⏭️ **Пропустить:**
   - Пометить сценарий как @Draft
   - Реализовать позже

**Ваш выбор?**
[ ] Добавить кастомный шаг
[ ] Переписать используя стандартные шаги
[ ] Пропустить как @Draft
```

---

### Тест 4.2: Все рекомендации семантически не подходят

**Входной сценарий:**
```gherkin
# encoding: utf-8
# language: ru

Функционал: Работа с деревом

Сценарий: Выбор узла дерева
    И я выбираю узел дерева "Корневой элемент"
```

**Результат валидатора:**
```yaml
errors:
  - line: 6
    suggestions:
      - text: 'И из выпадающего списка "ИмяСписка" я выбираю по строке "Значение"'
        similarity: 0.75
        semantic_match:
          action: true  # выбираю → выбираю
          element: false # дерево → список ❌
        warnings: ["Different element type: 'дерево' vs 'выпадающий список'"]
        
      - text: 'И в таблице "ИмяТаблицы" я выбираю текущую строку'
        similarity: 0.72
        semantic_match:
          action: true
          element: false # дерево → таблица ❌
        warnings: ["Different element type: 'дерево' vs 'таблица'"]
```

**Ожидаемое поведение AI:**
```
Анализ всех рекомендаций:

Рекомендация #1:
❌ Элемент: дерево ≠ выпадающий список
→ Отклонить

Рекомендация #2:
❌ Элемент: дерево ≠ таблица
→ Отклонить

❌ Все рекомендации семантически не подходят
→ Обратиться к пользователю
```

**Сообщение пользователю:**
```
🤝 Не могу автоматически исправить

**Строка 6:**
```gherkin
И я выбираю узел дерева "Корневой элемент"
```

**Рекомендации валидатора:**
1. `И из выпадающего списка "ИмяСписка" я выбираю по строке "Значение"` (75%)
   ❌ Не подходит: дерево ≠ выпадающий список

2. `И в таблице "ИмяТаблицы" я выбираю текущую строку` (72%)
   ❌ Не подходит: дерево ≠ таблица

**Мой анализ:**
Все рекомендации предлагают работу с другими типами элементов.
Для работы с деревом нужны специфичные шаги.

**Проверьте в библиотеке:**
- Есть ли шаги для работы с деревом?
- Возможно: `И в дереве "ИмяДерева" я...`

**Или используйте альтернативный подход:**
- Работа с деревом через таблицу (если применимо)
- Прямая навигация по ссылкам

Как поступить?
```

---

## Уровень 5: Зацикливание

### Тест 5.1: Классическое зацикливание

**Сценарий попыток:**

**Попытка #1:**
```gherkin
Ошибка: И я выбираю товар из справочника "Номенклатура"
```

Валидатор → рекомендация:
```
И из выпадающего списка "ИмяСписка" я выбираю по строке "Значение"
```

AI применяет:
```gherkin
И из выпадающего списка "Номенклатура" я выбираю по строке ""
```

❌ Ошибка: второй параметр пустой

---

**Попытка #2:**

AI добавляет параметр:
```gherkin
И из выпадающего списка "Номенклатура" я выбираю по строке "товар"
```

Валидатор:
```
❌ Шаг найден, но контекст некорректен
```

---

**Попытка #3:**

AI пытается другую рекомендацию:
```gherkin
И я нажимаю кнопку выбора у поля "Номенклатура"
```

Валидатор:
```
❌ Шаг валиден, но не завершён (нужны следующие шаги для выбора)
```

---

**Ожидаемое поведение AI после попытки #3:**

```python
if attempt_counter >= MAX_ATTEMPTS (3):
    escalate_to_human_with_history()
```

**Сообщение пользователю:**
```
🔄 Зациклился на исправлении

Не удалось автоматически исправить ошибку за 3 попытки.

**Исходный шаг (строка 8):**
```gherkin
И я выбираю товар из справочника "Номенклатура"
```

**История попыток:**

**Попытка #1:**
Применено: `И из выпадающего списка "Номенклатура" я выбираю по строке ""`
Результат: ❌ Второй параметр пустой

**Попытка #2:**
Применено: `И из выпадающего списка "Номенклатура" я выбираю по строке "товар"`
Результат: ❌ Контекст некорректен

**Попытка #3:**
Применено: `И я нажимаю кнопку выбора у поля "Номенклатура"`
Результат: ❌ Шаг неполный

---

**Мой анализ проблемы:**
Выбор элемента из справочника требует более сложной последовательности шагов:
1. Нажать кнопку выбора
2. Открыть форму подбора
3. Выбрать элемент в форме
4. Подтвердить выбор

Простой замены одного шага недостаточно.

**Рекомендуемые варианты:**

**Вариант 1: Развернутая последовательность**
```gherkin
И я нажимаю кнопку выбора у поля "Номенклатура"
Тогда открылось окно "Номенклатура"
И в таблице "Список" я перехожу к строке с условием 'Наименование = "Товар 1"'
И в таблице "Список" я выбираю текущую строку
```

**Вариант 2: Прямой ввод (если поддерживается)**
```gherkin
И в поле "Номенклатура" я ввожу текст "Товар 1"
```

**Вариант 3: Использование переменной с навигационной ссылкой**
```gherkin
И я открываю навигационную ссылку "$СсылкаНаНоменклатуру$"
```

**Какой вариант использовать?**
[ ] Вариант 1 (развернутая последовательность)
[ ] Вариант 2 (прямой ввод)
[ ] Вариант 3 (через переменную)
[ ] Другой (опишите)
```

---

## 📊 Матрица ожидаемых результатов

| Тест | Тип | Auto-fix | Semantic Check | Human-in-Loop | Успех |
|------|-----|----------|----------------|---------------|-------|
| 1.1 | Auto-fix | ✅ Да | - | - | ✅ |
| 1.2 | Auto-fix | ✅ Да | - | - | ✅ |
| 2.1 | Semantic | - | ✅ Pass | - | ✅ |
| 2.2 | Semantic | - | ✅ Pass | - | ✅ |
| 3.1 | Semantic | - | ✅ Pass (частично) | - | ✅ |
| 3.2 | Semantic | - | ❌ Fail на #2 | - | ✅ |
| 3.3 | Semantic | - | ⚠️ Ambiguous | ✅ Ask | ✅ |
| 4.1 | Critical | - | - | ✅ Escalate | ⏸️ |
| 4.2 | Critical | - | ❌ All fail | ✅ Escalate | ⏸️ |
| 5.1 | Loop | - | Mixed | ✅ Escalate | ⏸️ |

---

## 🎓 Выводы для AI-ассистента

### ✅ Что делать ВСЕГДА:
1. Вести счетчик попыток
2. Проверять ВСЕ 4 критерия семантики
3. Объяснять каждое исправление
4. Обращаться к пользователю при неуверенности

### ❌ Что НЕ делать НИКОГДА:
1. Применять рекомендации без проверки
2. Заменять действия (ввожу → выбираю)
3. Заменять типы элементов (кнопка → гиперссылка)
4. Зацикливаться (>3 попыток)

### 🤝 Когда обращаться к пользователю:
1. Нет подходящих рекомендаций
2. Все рекомендации семантически не подходят
3. Неоднозначная ситуация (несколько равных вариантов)
4. Зациклились (3 попытки исчерпаны)
5. Confidence < 60%

---

## 🔗 Связанные документы

- [`../../docs/architecture/ai-validator-integration.md`](../../docs/architecture/ai-validator-integration.md)
- [`../../ai-knowledge/semantic-validation-guide.md`](../../ai-knowledge/semantic-validation-guide.md)
- [`../../templates/prompts/ai-validator-workflow.md`](../../templates/prompts/ai-validator-workflow.md)
